# Forms PCF — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Criar o app `forms_pcf` com feedback anônimo de área e pedido de reembolso com fluxo de aprovação/rejeição e integração com `adm.Lancamento`.

**Architecture:** Novo Django app `forms_pcf` registrado em `INSTALLED_APPS`. Integra com `adm.Lancamento` (aprovação cria lançamento com `origem='REEMBOLSO'`) e `adm.Categoria`. O app `adm` recebe views de gestão de receptores de e-mail e um card "Reembolsos" no painel.

**Tech Stack:** Django 4.2, `send_mail`, `FileField` (MEDIA_ROOT), PCF design system (dark navy header, CSS vars shadcn/ui, Bootstrap 5).

## Global Constraints

- Templates estendem `base.html`; header `linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%)` com wave `clip-path:ellipse(55% 100% at 50% 100%)`; cor primária `#fe8210`
- Classes CSS prefixadas com `fp-` em `<style>` auto-contidos por template
- Login obrigatório em todas as URLs (`LoginRequiredMixin` ou `@login_required`)
- `timezone.now()` sempre — nunca `datetime.now()`
- Custom user model: `from voluntario.models import Voluntario` — nunca `auth.User`
- `adm.Lancamento.ORIGEM_CHOICES` deve conter `('REEMBOLSO', 'Reembolso')`
- Todo texto de UI em português brasileiro
- Comprovantes em `upload_to='reembolsos/'` (dentro de MEDIA_ROOT já configurado)
- E-mail via `django.core.mail.send_mail`; remetente `settings.DEFAULT_FROM_EMAIL`
- Permissões: feedback inbox → `area in ['PROJETOS','TRIADE']` ou superuser; reembolso inbox/aprovação → `area='ADM/FIN'` ou superuser

---

## File Map

**Criar:**
- `forms_pcf/__init__.py`
- `forms_pcf/apps.py`
- `forms_pcf/models.py`
- `forms_pcf/forms.py`
- `forms_pcf/views.py`
- `forms_pcf/urls.py`
- `forms_pcf/tests.py`
- `forms_pcf/migrations/__init__.py`
- `forms_pcf/templates/feedback_form.html`
- `forms_pcf/templates/feedback_sucesso.html`
- `forms_pcf/templates/feedback_inbox.html`
- `forms_pcf/templates/reembolso_form.html`
- `forms_pcf/templates/reembolso_sucesso.html`
- `forms_pcf/templates/reembolso_inbox.html`
- `adm/templates/receptores_reembolso.html`
- `adm/templates/form_receptor.html`

**Modificar:**
- `TESTE/settings.py` — adicionar `'forms_pcf'` em INSTALLED_APPS
- `TESTE/urls.py` — incluir `forms_pcf.urls`
- `adm/models.py` — adicionar `'REEMBOLSO'` em ORIGEM_CHOICES
- `adm/views.py` — adicionar views de receptores + atualizar `painel` com contagem
- `adm/urls.py` — adicionar URLs de receptores
- `adm/forms.py` — adicionar `ReceptorNotificacaoReembolsoForm`
- `adm/templates/painel_adm.html` — card Reembolsos

---

## Task 1: App scaffold, models e migrations

**Files:**
- Create: `forms_pcf/__init__.py`, `forms_pcf/apps.py`, `forms_pcf/models.py`, `forms_pcf/migrations/__init__.py`
- Modify: `TESTE/settings.py`, `adm/models.py`

**Interfaces:**
- Produces: `FeedbackArea`, `PedidoReembolso`, `ReceptorNotificacaoReembolso` models; `adm.ORIGEM_CHOICES` com `REEMBOLSO`

- [ ] **Step 1: Escrever o teste de modelos**

Crie `forms_pcf/tests.py`:

```python
from django.test import TestCase
from django.utils import timezone
from django.contrib.auth import get_user_model
from decimal import Decimal
from forms_pcf.models import FeedbackArea, PedidoReembolso, ReceptorNotificacaoReembolso
from adm.models import Categoria, Lancamento

User = get_user_model()


class FeedbackAreaModelTest(TestCase):
    def test_criado_sem_usuario(self):
        fb = FeedbackArea.objects.create(area='PROJETOS', descricao='Dor de teste')
        self.assertIsNone(getattr(fb, 'criado_por', None))
        self.assertIsNotNone(fb.criado_em)

    def test_str(self):
        fb = FeedbackArea(area='PROJETOS', descricao='Dor')
        self.assertIn('PROJETOS', str(fb))


class PedidoReembolsoModelTest(TestCase):
    def setUp(self):
        self.user = User.objects.create_user(
            username='vol', password='pw', area='MARKETING',
            first_name='Ana', last_name='Silva'
        )
        self.cat = Categoria.objects.create(nome='Transporte', tipo='DESPESA')

    def test_status_default_pendente(self):
        p = PedidoReembolso.objects.create(
            solicitante=self.user,
            valor=Decimal('50.00'),
            descricao='Uber',
            data_gasto=timezone.now().date(),
            categoria=self.cat,
            comprovante='reembolsos/fake.jpg',
        )
        self.assertEqual(p.status, 'PENDENTE')

    def test_str(self):
        p = PedidoReembolso(valor=Decimal('100.00'), status='PENDENTE')
        self.assertIn('100', str(p))


class ReceptorNotificacaoTest(TestCase):
    def test_str(self):
        r = ReceptorNotificacaoReembolso(nome='Maria', email='m@pcf.org')
        self.assertIn('Maria', str(r))

    def test_ativo_default(self):
        r = ReceptorNotificacaoReembolso.objects.create(nome='João', email='j@pcf.org')
        self.assertTrue(r.ativo)


class AdmOrigemReembolsoTest(TestCase):
    def test_origem_choices_contem_reembolso(self):
        from adm.models import ORIGEM_CHOICES
        valores = [v for v, _ in ORIGEM_CHOICES]
        self.assertIn('REEMBOLSO', valores)
```

- [ ] **Step 2: Rodar os testes — devem falhar**

```
python manage.py test forms_pcf adm.tests.AdmOrigemReembolsoTest -v 2
```
Esperado: `ModuleNotFoundError` ou `ImportError` (app não existe ainda).

- [ ] **Step 3: Criar a estrutura do app**

```
forms_pcf/__init__.py   (vazio)
```

`forms_pcf/apps.py`:
```python
from django.apps import AppConfig

class FormsPcfConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'forms_pcf'
    verbose_name = 'Formulários PCF'
```

`forms_pcf/migrations/__init__.py` (vazio)

- [ ] **Step 4: Criar `forms_pcf/models.py`**

```python
from django.db import models
from django.utils import timezone
from voluntario.models import LISTA_AREAS

STATUS_CHOICES = (
    ('PENDENTE', 'Pendente'),
    ('APROVADO', 'Aprovado'),
    ('REJEITADO', 'Rejeitado'),
)


class FeedbackArea(models.Model):
    area = models.CharField(max_length=30, choices=LISTA_AREAS)
    descricao = models.TextField()
    criado_em = models.DateTimeField(default=timezone.now)

    class Meta:
        ordering = ['-criado_em']
        verbose_name = 'Feedback de Área'
        verbose_name_plural = 'Feedbacks de Área'

    def __str__(self):
        return f'{self.area} — {self.criado_em:%d/%m/%Y}'


class PedidoReembolso(models.Model):
    solicitante = models.ForeignKey(
        'voluntario.Voluntario', on_delete=models.SET_NULL,
        null=True, related_name='pedidos_reembolso'
    )
    valor = models.DecimalField(max_digits=10, decimal_places=2)
    descricao = models.TextField()
    data_gasto = models.DateField()
    categoria = models.ForeignKey(
        'adm.Categoria', on_delete=models.PROTECT, related_name='pedidos_reembolso'
    )
    comprovante = models.FileField(upload_to='reembolsos/')
    status = models.CharField(max_length=15, choices=STATUS_CHOICES, default='PENDENTE')
    observacao_adm = models.TextField(blank=True)
    aprovado_por = models.ForeignKey(
        'voluntario.Voluntario', on_delete=models.SET_NULL,
        null=True, blank=True, related_name='reembolsos_aprovados'
    )
    aprovado_em = models.DateTimeField(null=True, blank=True)
    lancamento = models.OneToOneField(
        'adm.Lancamento', on_delete=models.SET_NULL,
        null=True, blank=True, related_name='pedido_reembolso'
    )
    criado_em = models.DateTimeField(default=timezone.now)

    class Meta:
        ordering = ['-criado_em']
        verbose_name = 'Pedido de Reembolso'
        verbose_name_plural = 'Pedidos de Reembolso'

    def __str__(self):
        return f'Reembolso R$ {self.valor} — {self.status}'


class ReceptorNotificacaoReembolso(models.Model):
    nome = models.CharField(max_length=100)
    email = models.EmailField(unique=True)
    ativo = models.BooleanField(default=True)

    class Meta:
        ordering = ['nome']
        verbose_name = 'Receptor de Notificação'
        verbose_name_plural = 'Receptores de Notificação'

    def __str__(self):
        return f'{self.nome} <{self.email}>'
```

- [ ] **Step 5: Adicionar `REEMBOLSO` em `adm/models.py`**

Em `adm/models.py`, altere `ORIGEM_CHOICES` de:
```python
ORIGEM_CHOICES = (
    ('MANUAL', 'Manual'),
    ('SUPPLY', 'Supply'),
)
```
para:
```python
ORIGEM_CHOICES = (
    ('MANUAL', 'Manual'),
    ('SUPPLY', 'Supply'),
    ('REEMBOLSO', 'Reembolso'),
)
```

- [ ] **Step 6: Registrar o app e fazer migrations**

Em `TESTE/settings.py`, em `INSTALLED_APPS`, após `'adm'`:
```python
    'forms_pcf',
```

Rodar:
```
python manage.py makemigrations adm
python manage.py makemigrations forms_pcf
python manage.py migrate
```
Esperado: migrations criadas e aplicadas sem erros.

- [ ] **Step 7: Rodar os testes — devem passar**

```
python manage.py test forms_pcf adm.tests.AdmOrigemReembolsoTest -v 2
```
Esperado: 5 testes passando.

- [ ] **Step 8: Commit**

```
git add forms_pcf/ adm/models.py adm/migrations/ TESTE/settings.py
git commit -m "feat(forms_pcf): scaffold app, models FeedbackArea/PedidoReembolso/Receptor e ORIGEM=REEMBOLSO"
```

---

## Task 2: Forms, views e templates do Feedback Anônimo

**Files:**
- Create: `forms_pcf/forms.py` (parcial), `forms_pcf/views.py` (parcial), `forms_pcf/urls.py`, `forms_pcf/templates/feedback_form.html`, `forms_pcf/templates/feedback_sucesso.html`, `forms_pcf/templates/feedback_inbox.html`
- Modify: `TESTE/urls.py`

**Interfaces:**
- Consumes: `FeedbackArea` model de Task 1
- Produces: `EnviarFeedbackView`, `FeedbackInboxView` acessíveis via `/forms/feedback/` e `/forms/feedback/inbox/`

- [ ] **Step 1: Escrever testes de feedback**

Adicione ao `forms_pcf/tests.py`:

```python
import io
from django.test import Client
from django.urls import reverse


class EnviarFeedbackViewTest(TestCase):
    def setUp(self):
        self.client = Client()
        self.user = User.objects.create_user(username='vol2', password='pw', area='MARKETING')
        self.client.force_login(self.user)

    def test_get_retorna_200(self):
        resp = self.client.get(reverse('forms_pcf:feedback'))
        self.assertEqual(resp.status_code, 200)

    def test_post_valido_cria_feedback_e_redireciona(self):
        resp = self.client.post(reverse('forms_pcf:feedback'), {
            'area': 'MARKETING',
            'descricao': 'Precisamos de mais comunicação entre áreas.',
        })
        self.assertRedirects(resp, reverse('forms_pcf:feedback_sucesso'))
        self.assertEqual(FeedbackArea.objects.count(), 1)
        fb = FeedbackArea.objects.first()
        self.assertEqual(fb.area, 'MARKETING')

    def test_post_invalido_nao_cria(self):
        resp = self.client.post(reverse('forms_pcf:feedback'), {'area': '', 'descricao': ''})
        self.assertEqual(resp.status_code, 200)
        self.assertEqual(FeedbackArea.objects.count(), 0)

    def test_anonimo_redireciona_login(self):
        self.client.logout()
        resp = self.client.get(reverse('forms_pcf:feedback'))
        self.assertEqual(resp.status_code, 302)
        self.assertIn('/login/', resp.url)


class FeedbackInboxPermissionTest(TestCase):
    def _login(self, area, superuser=False):
        c = Client()
        u = User.objects.create_user(
            username=f'u_{area}', password='pw', area=area, is_superuser=superuser
        )
        c.force_login(u)
        return c

    def test_projetos_tem_acesso(self):
        resp = self._login('PROJETOS').get(reverse('forms_pcf:feedback_inbox'))
        self.assertEqual(resp.status_code, 200)

    def test_triade_tem_acesso(self):
        resp = self._login('TRIADE').get(reverse('forms_pcf:feedback_inbox'))
        self.assertEqual(resp.status_code, 200)

    def test_superuser_tem_acesso(self):
        resp = self._login('MARKETING', superuser=True).get(reverse('forms_pcf:feedback_inbox'))
        self.assertEqual(resp.status_code, 200)

    def test_outros_recebem_403(self):
        resp = self._login('MARKETING').get(reverse('forms_pcf:feedback_inbox'))
        self.assertEqual(resp.status_code, 403)
```

- [ ] **Step 2: Rodar — devem falhar**

```
python manage.py test forms_pcf.tests.EnviarFeedbackViewTest forms_pcf.tests.FeedbackInboxPermissionTest -v 2
```
Esperado: `NoReverseMatch` (URLs não existem ainda).

- [ ] **Step 3: Criar `forms_pcf/forms.py`**

```python
from django import forms
from .models import FeedbackArea, PedidoReembolso
from adm.models import Categoria


class FeedbackAreaForm(forms.ModelForm):
    class Meta:
        model = FeedbackArea
        fields = ['area', 'descricao']
        widgets = {
            'area': forms.Select(attrs={'class': 'form-select'}),
            'descricao': forms.Textarea(attrs={
                'class': 'form-control', 'rows': 5,
                'placeholder': 'Descreva a dor ou problema da sua área...'
            }),
        }
        labels = {
            'area': 'Área',
            'descricao': 'Descrição',
        }


class PedidoReembolsoForm(forms.ModelForm):
    class Meta:
        model = PedidoReembolso
        fields = ['valor', 'descricao', 'data_gasto', 'categoria', 'comprovante']
        widgets = {
            'valor': forms.NumberInput(attrs={'class': 'form-control', 'step': '0.01', 'min': '0.01'}),
            'descricao': forms.Textarea(attrs={'class': 'form-control', 'rows': 3}),
            'data_gasto': forms.DateInput(attrs={'class': 'form-control', 'type': 'date'}),
            'categoria': forms.Select(attrs={'class': 'form-select'}),
            'comprovante': forms.ClearableFileInput(attrs={'class': 'form-control'}),
        }
        labels = {
            'valor': 'Valor (R$)',
            'descricao': 'Descrição do gasto',
            'data_gasto': 'Data do gasto',
            'categoria': 'Categoria',
            'comprovante': 'Comprovante (foto ou PDF)',
        }

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        self.fields['categoria'].queryset = Categoria.objects.filter(tipo='DESPESA', ativo=True)
```

- [ ] **Step 4: Criar `forms_pcf/views.py` (só feedback por ora)**

```python
from django.contrib.auth.mixins import LoginRequiredMixin
from django.core.exceptions import PermissionDenied
from django.views.generic import FormView, ListView, TemplateView
from django.urls import reverse_lazy
from .models import FeedbackArea
from .forms import FeedbackAreaForm

FEEDBACK_AREAS = {'PROJETOS', 'TRIADE'}


class EnviarFeedbackView(LoginRequiredMixin, FormView):
    template_name = 'feedback_form.html'
    form_class = FeedbackAreaForm
    success_url = reverse_lazy('forms_pcf:feedback_sucesso')

    def form_valid(self, form):
        form.save()
        return super().form_valid(form)


class FeedbackSucessoView(LoginRequiredMixin, TemplateView):
    template_name = 'feedback_sucesso.html'


class FeedbackInboxView(LoginRequiredMixin, ListView):
    template_name = 'feedback_inbox.html'
    context_object_name = 'feedbacks'

    def dispatch(self, request, *args, **kwargs):
        if not request.user.is_authenticated:
            return self.handle_no_permission()
        if not (request.user.is_superuser or getattr(request.user, 'area', None) in FEEDBACK_AREAS):
            raise PermissionDenied
        return super().dispatch(request, *args, **kwargs)

    def get_queryset(self):
        return FeedbackArea.objects.order_by('-criado_em')
```

- [ ] **Step 5: Criar `forms_pcf/urls.py`**

```python
from django.urls import path
from . import views

app_name = 'forms_pcf'

urlpatterns = [
    path('feedback/', views.EnviarFeedbackView.as_view(), name='feedback'),
    path('feedback/sucesso/', views.FeedbackSucessoView.as_view(), name='feedback_sucesso'),
    path('feedback/inbox/', views.FeedbackInboxView.as_view(), name='feedback_inbox'),
]
```

- [ ] **Step 6: Registrar em `TESTE/urls.py`**

Após `path('adm/', include('adm.urls', namespace='adm')),` adicione:
```python
    path('forms/', include('forms_pcf.urls', namespace='forms_pcf')),
```
Adicione também o import no topo se necessário: já existe `from django.urls import path, include`.

- [ ] **Step 7: Criar `forms_pcf/templates/feedback_form.html`**

```html
{% extends 'base.html' %}
{% block titulo %}Feedback de Área — PCF{% endblock %}

{% block content %}
<div class="fp-page">
  <div class="fp-header">
    <div class="fp-container">
      <p class="fp-breadcrumb">
        <a href="{% url 'inicio' %}" class="fp-breadcrumb-link">Início</a>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
        Feedback de Área
      </p>
      <h1 class="fp-title">Feedback Anônimo</h1>
      <p class="fp-sub">Relate dores e desafios da sua área. Sua identidade não será registrada.</p>
    </div>
  </div>

  <div class="fp-container fp-body">
    <div class="fp-card">
      <form method="post">
        {% csrf_token %}
        <div class="fp-field">
          <label class="fp-label">{{ form.area.label }}</label>
          {{ form.area }}
          {% if form.area.errors %}<p class="fp-error">{{ form.area.errors|join:", " }}</p>{% endif %}
        </div>
        <div class="fp-field">
          <label class="fp-label">{{ form.descricao.label }}</label>
          {{ form.descricao }}
          {% if form.descricao.errors %}<p class="fp-error">{{ form.descricao.errors|join:", " }}</p>{% endif %}
        </div>
        <div class="fp-actions">
          <button type="submit" class="fp-btn fp-btn-primary">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
            Enviar Feedback
          </button>
          <a href="{% url 'inicio' %}" class="fp-btn fp-btn-outline">Cancelar</a>
        </div>
      </form>
    </div>
  </div>
</div>

<style>
*,*::before,*::after{box-sizing:border-box}
.fp-page{background:hsl(var(--background));min-height:100vh}
.fp-container{max-width:560px;margin:0 auto;padding:0 1.5rem}
.fp-header{background:linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%);padding:2.25rem 0 2.75rem;position:relative;overflow:hidden}
.fp-header::after{content:'';position:absolute;bottom:-2px;left:0;right:0;height:48px;background:hsl(var(--background));clip-path:ellipse(55% 100% at 50% 100%)}
.fp-breadcrumb{display:inline-flex;align-items:center;gap:.375rem;color:rgba(255,255,255,.45);font-size:.72rem;font-weight:600;letter-spacing:.06em;text-transform:uppercase;margin:0 0 .75rem;position:relative;z-index:1}
.fp-breadcrumb-link{color:rgba(255,255,255,.55);text-decoration:none}
.fp-breadcrumb-link:hover{color:#fff}
.fp-title{font-size:clamp(1.25rem,3vw,1.75rem);font-weight:800;color:#fff;margin:0 0 .4rem;line-height:1.15;position:relative;z-index:1}
.fp-sub{font-size:.875rem;color:rgba(255,255,255,.55);margin:0;position:relative;z-index:1}
.fp-body{padding-top:1.75rem;padding-bottom:4rem}
.fp-card{background:hsl(var(--card));border:1px solid hsl(var(--border));border-radius:1rem;padding:1.75rem;box-shadow:0 1px 3px rgba(0,0,0,.06)}
.fp-field{margin-bottom:1.25rem}
.fp-label{display:block;font-size:.8rem;font-weight:700;color:hsl(var(--muted-foreground));text-transform:uppercase;letter-spacing:.05em;margin-bottom:.45rem}
.fp-card select,.fp-card textarea{width:100%;padding:.625rem .875rem;border:1px solid hsl(var(--border));border-radius:.5rem;background:hsl(var(--background));color:hsl(var(--foreground));font-size:.9rem;outline:none;transition:border .15s;font-family:inherit}
.fp-card select:focus,.fp-card textarea:focus{border-color:#fe8210;box-shadow:0 0 0 3px rgba(254,130,16,.12)}
.fp-card textarea{min-height:120px;resize:vertical}
.fp-error{font-size:.78rem;color:#dc2626;font-weight:600;margin:.3rem 0 0}
.fp-actions{display:flex;gap:.75rem;padding-top:.25rem}
.fp-btn{display:inline-flex;align-items:center;gap:.45rem;padding:.625rem 1.25rem;border-radius:.5rem;font-size:.875rem;font-weight:600;text-decoration:none;border:none;cursor:pointer;transition:all .15s;white-space:nowrap}
.fp-btn-primary{background:#fe8210;color:#fff}
.fp-btn-primary:hover{background:#e87207;color:#fff;transform:translateY(-1px);box-shadow:0 4px 14px rgba(254,130,16,.4)}
.fp-btn-outline{background:transparent;color:hsl(var(--foreground));border:1px solid hsl(var(--border))}
.fp-btn-outline:hover{background:hsl(var(--accent))}
@media(max-width:520px){.fp-container{padding:0 1rem}.fp-card{padding:1.25rem}.fp-actions{flex-direction:column}}
</style>
{% endblock %}
```

- [ ] **Step 8: Criar `forms_pcf/templates/feedback_sucesso.html`**

```html
{% extends 'base.html' %}
{% block titulo %}Feedback Enviado — PCF{% endblock %}

{% block content %}
<div class="fp-page">
  <div class="fp-header">
    <div class="fp-container">
      <h1 class="fp-title">Feedback Enviado!</h1>
      <p class="fp-sub">Obrigado pela sua contribuição. Sua resposta é anônima.</p>
    </div>
  </div>
  <div class="fp-container fp-body" style="text-align:center">
    <div class="fp-success-icon">
      <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="#059669" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
    </div>
    <p style="color:hsl(var(--muted-foreground));margin:.75rem 0 1.5rem;font-size:.9rem">Seu feedback foi registrado com sucesso e será analisado pela equipe de Projetos.</p>
    <a href="{% url 'inicio' %}" class="fp-btn fp-btn-primary">Voltar ao Início</a>
    <a href="{% url 'forms_pcf:feedback' %}" class="fp-btn fp-btn-outline" style="margin-top:.5rem">Enviar outro feedback</a>
  </div>
</div>

<style>
*,*::before,*::after{box-sizing:border-box}
.fp-page{background:hsl(var(--background));min-height:100vh}
.fp-container{max-width:480px;margin:0 auto;padding:0 1.5rem}
.fp-header{background:linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%);padding:2.25rem 0 2.75rem;position:relative;overflow:hidden}
.fp-header::after{content:'';position:absolute;bottom:-2px;left:0;right:0;height:48px;background:hsl(var(--background));clip-path:ellipse(55% 100% at 50% 100%)}
.fp-title{font-size:clamp(1.25rem,3vw,1.75rem);font-weight:800;color:#fff;margin:0 0 .4rem;line-height:1.15;position:relative;z-index:1}
.fp-sub{font-size:.875rem;color:rgba(255,255,255,.55);margin:0;position:relative;z-index:1}
.fp-body{padding-top:2.5rem;padding-bottom:4rem}
.fp-success-icon{width:72px;height:72px;background:#d1fae5;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto .5rem}
.fp-btn{display:inline-flex;align-items:center;gap:.45rem;padding:.625rem 1.25rem;border-radius:.5rem;font-size:.875rem;font-weight:600;text-decoration:none;border:none;cursor:pointer;transition:all .15s;white-space:nowrap}
.fp-btn-primary{background:#fe8210;color:#fff;display:block}
.fp-btn-primary:hover{background:#e87207;color:#fff}
.fp-btn-outline{background:transparent;color:hsl(var(--foreground));border:1px solid hsl(var(--border));display:block}
.fp-btn-outline:hover{background:hsl(var(--accent))}
</style>
{% endblock %}
```

- [ ] **Step 9: Criar `forms_pcf/templates/feedback_inbox.html`**

```html
{% extends 'base.html' %}
{% block titulo %}Inbox de Feedbacks — PCF{% endblock %}

{% block content %}
<div class="fp-page">
  <div class="fp-header">
    <div class="fp-container-wide">
      <div class="fp-header-inner">
        <div>
          <p class="fp-breadcrumb">
            <a href="{% url 'inicio' %}" class="fp-breadcrumb-link">Início</a>
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
            Inbox de Feedbacks
          </p>
          <h1 class="fp-title">Feedbacks de Área</h1>
          <p class="fp-sub">Dores e desafios relatados pelas áreas do PCF.</p>
        </div>
      </div>
    </div>
  </div>

  <div class="fp-container-wide fp-body">
    <div class="fp-inbox-card">
      {% for fb in feedbacks %}
      <div class="fp-inbox-item" data-id="{{ fb.pk }}">
        <div class="fp-inbox-meta">
          <span class="fp-area-badge">{{ fb.get_area_display }}</span>
          <span class="fp-inbox-date">{{ fb.criado_em|date:"d/m/Y H:i" }}</span>
          <button class="fp-toggle-btn" onclick="toggleFeedback({{ fb.pk }})">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m6 9 6 6 6-6"/></svg>
          </button>
        </div>
        <p class="fp-inbox-preview">{{ fb.descricao|truncatechars:120 }}</p>
        <div class="fp-inbox-full" id="full-{{ fb.pk }}" style="display:none">
          <p class="fp-inbox-full-text">{{ fb.descricao }}</p>
        </div>
      </div>
      {% empty %}
      <div class="fp-empty">
        <div class="fp-empty-icon">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
        </div>
        <p class="fp-empty-title">Nenhum feedback recebido ainda</p>
        <p class="fp-empty-sub">Quando as áreas enviarem feedbacks, eles aparecerão aqui.</p>
      </div>
      {% endfor %}
    </div>
  </div>
</div>

<script>
function toggleFeedback(id) {
  var el = document.getElementById('full-' + id);
  var prev = el.previousElementSibling;
  if (el.style.display === 'none') {
    el.style.display = 'block';
    if (prev && prev.classList.contains('fp-inbox-preview')) prev.style.display = 'none';
  } else {
    el.style.display = 'none';
    if (prev && prev.classList.contains('fp-inbox-preview')) prev.style.display = '';
  }
}
</script>

<style>
*,*::before,*::after{box-sizing:border-box}
.fp-page{background:hsl(var(--background));min-height:100vh}
.fp-container-wide{max-width:860px;margin:0 auto;padding:0 1.5rem}
.fp-header{background:linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%);padding:2.25rem 0 2.75rem;position:relative;overflow:hidden}
.fp-header::after{content:'';position:absolute;bottom:-2px;left:0;right:0;height:48px;background:hsl(var(--background));clip-path:ellipse(55% 100% at 50% 100%)}
.fp-header-inner{position:relative;z-index:1}
.fp-breadcrumb{display:inline-flex;align-items:center;gap:.375rem;color:rgba(255,255,255,.45);font-size:.72rem;font-weight:600;letter-spacing:.06em;text-transform:uppercase;margin:0 0 .75rem}
.fp-breadcrumb-link{color:rgba(255,255,255,.55);text-decoration:none}
.fp-breadcrumb-link:hover{color:#fff}
.fp-title{font-size:clamp(1.375rem,3vw,1.875rem);font-weight:800;color:#fff;margin:0 0 .4rem;line-height:1.15}
.fp-sub{font-size:.875rem;color:rgba(255,255,255,.55);margin:0}
.fp-body{padding-top:1.75rem;padding-bottom:4rem}
.fp-inbox-card{background:hsl(var(--card));border:1px solid hsl(var(--border));border-radius:1rem;overflow:hidden;box-shadow:0 1px 3px rgba(0,0,0,.06)}
.fp-inbox-item{padding:1.125rem 1.375rem;border-bottom:1px solid hsl(var(--border))}
.fp-inbox-item:last-child{border-bottom:none}
.fp-inbox-meta{display:flex;align-items:center;gap:.75rem;margin-bottom:.5rem}
.fp-area-badge{font-size:.72rem;font-weight:700;background:hsl(var(--accent));color:hsl(var(--foreground));padding:.25rem .65rem;border-radius:9999px;border:1px solid hsl(var(--border))}
.fp-inbox-date{font-size:.78rem;color:hsl(var(--muted-foreground));margin-left:auto}
.fp-toggle-btn{background:none;border:none;cursor:pointer;color:hsl(var(--muted-foreground));padding:.25rem;display:flex;align-items:center;border-radius:.25rem}
.fp-toggle-btn:hover{background:hsl(var(--accent))}
.fp-inbox-preview{font-size:.875rem;color:hsl(var(--foreground));margin:0;line-height:1.55}
.fp-inbox-full-text{font-size:.875rem;color:hsl(var(--foreground));margin:0;line-height:1.6;white-space:pre-wrap}
.fp-empty{padding:3rem 2rem;text-align:center}
.fp-empty-icon{width:48px;height:48px;background:hsl(var(--accent));border-radius:.875rem;display:flex;align-items:center;justify-content:center;color:hsl(var(--muted-foreground));margin:0 auto .75rem}
.fp-empty-title{font-size:.9375rem;font-weight:600;color:hsl(var(--foreground));margin:0 0 .35rem}
.fp-empty-sub{font-size:.825rem;color:hsl(var(--muted-foreground));margin:0}
@media(max-width:768px){.fp-container-wide{padding:0 1rem}}
</style>
{% endblock %}
```

- [ ] **Step 10: Rodar testes — devem passar**

```
python manage.py test forms_pcf.tests.EnviarFeedbackViewTest forms_pcf.tests.FeedbackInboxPermissionTest -v 2
```
Esperado: 8 testes passando.

- [ ] **Step 11: Commit**

```
git add forms_pcf/
git commit -m "feat(forms_pcf): feedback anônimo — form, views, templates e testes"
```

---

## Task 3: Views e templates do Reembolso (enviar + inbox + aprovar + rejeitar)

**Files:**
- Modify: `forms_pcf/views.py` (adicionar views de reembolso), `forms_pcf/urls.py` (adicionar URLs)
- Create: `forms_pcf/templates/reembolso_form.html`, `forms_pcf/templates/reembolso_sucesso.html`, `forms_pcf/templates/reembolso_inbox.html`

**Interfaces:**
- Consumes: `PedidoReembolso`, `ReceptorNotificacaoReembolso`, `PedidoReembolsoForm` de Tasks 1–2; `adm.Lancamento`, `adm.Categoria`
- Produces: `EnviarReembolsoView`, `ReembolsoInboxView`, `AprovarReembolsoView`, `RejeitarReembolsoView`

- [ ] **Step 1: Escrever testes de reembolso**

Adicione ao `forms_pcf/tests.py`:

```python
from unittest.mock import patch
from django.core.files.uploadedfile import SimpleUploadedFile
from forms_pcf.models import ReceptorNotificacaoReembolso


class EnviarReembolsoViewTest(TestCase):
    def setUp(self):
        self.client = Client()
        self.user = User.objects.create_user(username='req', password='pw', area='MARKETING')
        self.client.force_login(self.user)
        self.cat = Categoria.objects.create(nome='Transporte', tipo='DESPESA')
        ReceptorNotificacaoReembolso.objects.create(nome='ADM1', email='adm@pcf.org', ativo=True)
        ReceptorNotificacaoReembolso.objects.create(nome='ADM2', email='adm2@pcf.org', ativo=False)

    @patch('forms_pcf.views.send_mail')
    def test_post_valido_cria_pedido_e_envia_email(self, mock_mail):
        arquivo = SimpleUploadedFile('comp.jpg', b'fake', content_type='image/jpeg')
        resp = self.client.post(reverse('forms_pcf:reembolso'), {
            'valor': '75.50',
            'descricao': 'Uber para evento',
            'data_gasto': timezone.now().date().isoformat(),
            'categoria': self.cat.pk,
            'comprovante': arquivo,
        })
        self.assertRedirects(resp, reverse('forms_pcf:reembolso_sucesso'))
        self.assertEqual(PedidoReembolso.objects.count(), 1)
        pedido = PedidoReembolso.objects.first()
        self.assertEqual(pedido.status, 'PENDENTE')
        self.assertEqual(pedido.solicitante, self.user)
        # Só o receptor ativo deve receber
        self.assertEqual(mock_mail.call_count, 1)
        call_kwargs = mock_mail.call_args
        self.assertIn('adm@pcf.org', call_kwargs[1].get('recipient_list', call_kwargs[0][3] if len(call_kwargs[0]) > 3 else []))

    def test_sem_comprovante_nao_cria(self):
        resp = self.client.post(reverse('forms_pcf:reembolso'), {
            'valor': '10.00',
            'descricao': 'Sem comp',
            'data_gasto': timezone.now().date().isoformat(),
            'categoria': self.cat.pk,
        })
        self.assertEqual(resp.status_code, 200)
        self.assertEqual(PedidoReembolso.objects.count(), 0)


class AprovarReembolsoViewTest(TestCase):
    def setUp(self):
        self.client = Client()
        self.adm = User.objects.create_user(username='adm', password='pw', area='ADM/FIN')
        self.client.force_login(self.adm)
        self.cat = Categoria.objects.create(nome='Material', tipo='DESPESA')
        self.pedido = PedidoReembolso.objects.create(
            solicitante=self.adm,
            valor=Decimal('120.00'),
            descricao='Materiais',
            data_gasto=timezone.now().date(),
            categoria=self.cat,
            comprovante='reembolsos/fake.jpg',
            status='PENDENTE',
        )

    def test_aprovacao_cria_lancamento_e_atualiza_status(self):
        resp = self.client.post(reverse('forms_pcf:reembolso_aprovar', args=[self.pedido.pk]))
        self.assertRedirects(resp, reverse('forms_pcf:reembolso_inbox'))
        self.pedido.refresh_from_db()
        self.assertEqual(self.pedido.status, 'APROVADO')
        self.assertIsNotNone(self.pedido.lancamento)
        lan = self.pedido.lancamento
        self.assertEqual(lan.origem, 'REEMBOLSO')
        self.assertEqual(lan.valor, Decimal('120.00'))
        self.assertEqual(lan.tipo, 'DESPESA')

    def test_nao_adm_recebe_403(self):
        outro = User.objects.create_user(username='out', password='pw', area='MARKETING')
        c = Client()
        c.force_login(outro)
        resp = c.post(reverse('forms_pcf:reembolso_aprovar', args=[self.pedido.pk]))
        self.assertEqual(resp.status_code, 403)


class RejeitarReembolsoViewTest(TestCase):
    def setUp(self):
        self.client = Client()
        self.adm = User.objects.create_user(username='adm2', password='pw', area='ADM/FIN')
        self.client.force_login(self.adm)
        self.cat = Categoria.objects.create(nome='Outro', tipo='DESPESA')
        self.pedido = PedidoReembolso.objects.create(
            solicitante=self.adm,
            valor=Decimal('30.00'),
            descricao='Gasto',
            data_gasto=timezone.now().date(),
            categoria=self.cat,
            comprovante='reembolsos/fake.jpg',
            status='PENDENTE',
        )

    def test_rejeicao_com_motivo(self):
        resp = self.client.post(
            reverse('forms_pcf:reembolso_rejeitar', args=[self.pedido.pk]),
            {'observacao_adm': 'Comprovante ilegível'}
        )
        self.assertRedirects(resp, reverse('forms_pcf:reembolso_inbox'))
        self.pedido.refresh_from_db()
        self.assertEqual(self.pedido.status, 'REJEITADO')
        self.assertEqual(self.pedido.observacao_adm, 'Comprovante ilegível')

    def test_rejeicao_sem_motivo_nao_rejeita(self):
        resp = self.client.post(
            reverse('forms_pcf:reembolso_rejeitar', args=[self.pedido.pk]),
            {'observacao_adm': ''}
        )
        self.assertEqual(resp.status_code, 302)
        self.pedido.refresh_from_db()
        self.assertEqual(self.pedido.status, 'PENDENTE')


class ReembolsoInboxPermissionTest(TestCase):
    def _login(self, area, superuser=False):
        c = Client()
        u = User.objects.create_user(
            username=f'ui_{area}', password='pw', area=area, is_superuser=superuser
        )
        c.force_login(u)
        return c

    def test_adm_fin_tem_acesso(self):
        resp = self._login('ADM/FIN').get(reverse('forms_pcf:reembolso_inbox'))
        self.assertEqual(resp.status_code, 200)

    def test_superuser_tem_acesso(self):
        resp = self._login('MARKETING', superuser=True).get(reverse('forms_pcf:reembolso_inbox'))
        self.assertEqual(resp.status_code, 200)

    def test_outros_recebem_403(self):
        resp = self._login('MARKETING').get(reverse('forms_pcf:reembolso_inbox'))
        self.assertEqual(resp.status_code, 403)
```

- [ ] **Step 2: Rodar — devem falhar**

```
python manage.py test forms_pcf.tests.EnviarReembolsoViewTest forms_pcf.tests.AprovarReembolsoViewTest forms_pcf.tests.RejeitarReembolsoViewTest forms_pcf.tests.ReembolsoInboxPermissionTest -v 2
```
Esperado: `NoReverseMatch` (URLs não existem ainda).

- [ ] **Step 3: Adicionar views de reembolso em `forms_pcf/views.py`**

Acrescente ao arquivo (mantendo o que existe):

```python
from django.views import View
from django.http import Http404
from django.shortcuts import get_object_or_404, redirect
from django.conf import settings
from django.core.mail import send_mail
from django.utils import timezone
from .models import PedidoReembolso, ReceptorNotificacaoReembolso
from .forms import PedidoReembolsoForm
from adm.models import Lancamento

REEMBOLSO_AREAS = {'ADM/FIN'}


class EnviarReembolsoView(LoginRequiredMixin, FormView):
    template_name = 'reembolso_form.html'
    form_class = PedidoReembolsoForm
    success_url = reverse_lazy('forms_pcf:reembolso_sucesso')

    def get_form_kwargs(self):
        kwargs = super().get_form_kwargs()
        if self.request.method in ('POST', 'PUT'):
            kwargs['files'] = self.request.FILES
        return kwargs

    def form_valid(self, form):
        pedido = form.save(commit=False)
        pedido.solicitante = self.request.user
        pedido.status = 'PENDENTE'
        pedido.save()
        self._enviar_email(pedido)
        return super().form_valid(form)

    def _enviar_email(self, pedido):
        receptores = list(
            ReceptorNotificacaoReembolso.objects.filter(ativo=True).values_list('email', flat=True)
        )
        if not receptores:
            return
        nome = pedido.solicitante.get_full_name() or pedido.solicitante.username
        area = getattr(pedido.solicitante, 'area', '—')
        assunto = f'[PCF] Novo pedido de reembolso — R$ {pedido.valor}'
        corpo = (
            f'Novo pedido de reembolso recebido.\n\n'
            f'Solicitante: {nome}\n'
            f'Área: {area}\n'
            f'Valor: R$ {pedido.valor}\n'
            f'Data do gasto: {pedido.data_gasto:%d/%m/%Y}\n'
            f'Categoria: {pedido.categoria.nome}\n'
            f'Descrição: {pedido.descricao}\n\n'
            f'Acesse o sistema para aprovar ou rejeitar.'
        )
        send_mail(assunto, corpo, settings.DEFAULT_FROM_EMAIL, receptores, fail_silently=True)


class ReembolsoSucessoView(LoginRequiredMixin, TemplateView):
    template_name = 'reembolso_sucesso.html'


class ReembolsoInboxView(LoginRequiredMixin, ListView):
    template_name = 'reembolso_inbox.html'
    context_object_name = 'pedidos'

    def dispatch(self, request, *args, **kwargs):
        if not request.user.is_authenticated:
            return self.handle_no_permission()
        if not (request.user.is_superuser or getattr(request.user, 'area', None) in REEMBOLSO_AREAS):
            raise PermissionDenied
        return super().dispatch(request, *args, **kwargs)

    def get_queryset(self):
        status = self.request.GET.get('status', 'PENDENTE')
        if status not in ('PENDENTE', 'APROVADO', 'REJEITADO'):
            status = 'PENDENTE'
        return PedidoReembolso.objects.filter(status=status).select_related(
            'solicitante', 'categoria', 'aprovado_por'
        )

    def get_context_data(self, **kwargs):
        ctx = super().get_context_data(**kwargs)
        ctx['status_ativo'] = self.request.GET.get('status', 'PENDENTE')
        ctx['contagem_pendente'] = PedidoReembolso.objects.filter(status='PENDENTE').count()
        ctx['contagem_aprovado'] = PedidoReembolso.objects.filter(status='APROVADO').count()
        ctx['contagem_rejeitado'] = PedidoReembolso.objects.filter(status='REJEITADO').count()
        return ctx


class AprovarReembolsoView(LoginRequiredMixin, View):
    def dispatch(self, request, *args, **kwargs):
        if not request.user.is_authenticated:
            return self.handle_no_permission()
        if not (request.user.is_superuser or getattr(request.user, 'area', None) in REEMBOLSO_AREAS):
            raise PermissionDenied
        return super().dispatch(request, *args, **kwargs)

    def post(self, request, pk):
        pedido = get_object_or_404(PedidoReembolso, pk=pk, status='PENDENTE')
        lan = Lancamento.objects.create(
            categoria=pedido.categoria,
            valor=pedido.valor,
            data=timezone.now().date(),
            descricao=f'Reembolso: {pedido.descricao}',
            origem='REEMBOLSO',
            criado_por=request.user,
        )
        pedido.status = 'APROVADO'
        pedido.aprovado_por = request.user
        pedido.aprovado_em = timezone.now()
        pedido.lancamento = lan
        pedido.save()
        return redirect('forms_pcf:reembolso_inbox')


class RejeitarReembolsoView(LoginRequiredMixin, View):
    def dispatch(self, request, *args, **kwargs):
        if not request.user.is_authenticated:
            return self.handle_no_permission()
        if not (request.user.is_superuser or getattr(request.user, 'area', None) in REEMBOLSO_AREAS):
            raise PermissionDenied
        return super().dispatch(request, *args, **kwargs)

    def post(self, request, pk):
        pedido = get_object_or_404(PedidoReembolso, pk=pk, status='PENDENTE')
        motivo = request.POST.get('observacao_adm', '').strip()
        if not motivo:
            return redirect('forms_pcf:reembolso_inbox')
        pedido.status = 'REJEITADO'
        pedido.observacao_adm = motivo
        pedido.aprovado_por = request.user
        pedido.aprovado_em = timezone.now()
        pedido.save()
        return redirect('forms_pcf:reembolso_inbox')
```

- [ ] **Step 4: Adicionar URLs de reembolso em `forms_pcf/urls.py`**

Substitua o conteúdo completo do arquivo:

```python
from django.urls import path
from . import views

app_name = 'forms_pcf'

urlpatterns = [
    path('feedback/', views.EnviarFeedbackView.as_view(), name='feedback'),
    path('feedback/sucesso/', views.FeedbackSucessoView.as_view(), name='feedback_sucesso'),
    path('feedback/inbox/', views.FeedbackInboxView.as_view(), name='feedback_inbox'),
    path('reembolso/', views.EnviarReembolsoView.as_view(), name='reembolso'),
    path('reembolso/sucesso/', views.ReembolsoSucessoView.as_view(), name='reembolso_sucesso'),
    path('reembolso/inbox/', views.ReembolsoInboxView.as_view(), name='reembolso_inbox'),
    path('reembolso/<int:pk>/aprovar/', views.AprovarReembolsoView.as_view(), name='reembolso_aprovar'),
    path('reembolso/<int:pk>/rejeitar/', views.RejeitarReembolsoView.as_view(), name='reembolso_rejeitar'),
]
```

- [ ] **Step 5: Criar `forms_pcf/templates/reembolso_form.html`**

```html
{% extends 'base.html' %}
{% block titulo %}Pedido de Reembolso — PCF{% endblock %}

{% block content %}
<div class="fp-page">
  <div class="fp-header">
    <div class="fp-container">
      <p class="fp-breadcrumb">
        <a href="{% url 'inicio' %}" class="fp-breadcrumb-link">Início</a>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
        Pedido de Reembolso
      </p>
      <h1 class="fp-title">Pedido de Reembolso</h1>
      <p class="fp-sub">Preencha os dados do gasto e anexe o comprovante. O ADM/FIN irá analisar sua solicitação.</p>
    </div>
  </div>

  <div class="fp-container fp-body">
    <div class="fp-card">
      <form method="post" enctype="multipart/form-data">
        {% csrf_token %}
        {% for field in form %}
        <div class="fp-field">
          <label class="fp-label">{{ field.label }}</label>
          {{ field }}
          {% if field.help_text %}<p class="fp-help">{{ field.help_text }}</p>{% endif %}
          {% if field.errors %}<p class="fp-error">{{ field.errors|join:", " }}</p>{% endif %}
        </div>
        {% endfor %}
        <div class="fp-actions">
          <button type="submit" class="fp-btn fp-btn-primary">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
            Enviar Pedido
          </button>
          <a href="{% url 'inicio' %}" class="fp-btn fp-btn-outline">Cancelar</a>
        </div>
      </form>
    </div>
  </div>
</div>

<style>
*,*::before,*::after{box-sizing:border-box}
.fp-page{background:hsl(var(--background));min-height:100vh}
.fp-container{max-width:560px;margin:0 auto;padding:0 1.5rem}
.fp-header{background:linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%);padding:2.25rem 0 2.75rem;position:relative;overflow:hidden}
.fp-header::after{content:'';position:absolute;bottom:-2px;left:0;right:0;height:48px;background:hsl(var(--background));clip-path:ellipse(55% 100% at 50% 100%)}
.fp-breadcrumb{display:inline-flex;align-items:center;gap:.375rem;color:rgba(255,255,255,.45);font-size:.72rem;font-weight:600;letter-spacing:.06em;text-transform:uppercase;margin:0 0 .75rem;position:relative;z-index:1}
.fp-breadcrumb-link{color:rgba(255,255,255,.55);text-decoration:none}
.fp-breadcrumb-link:hover{color:#fff}
.fp-title{font-size:clamp(1.25rem,3vw,1.75rem);font-weight:800;color:#fff;margin:0 0 .4rem;line-height:1.15;position:relative;z-index:1}
.fp-sub{font-size:.875rem;color:rgba(255,255,255,.55);margin:0;position:relative;z-index:1}
.fp-body{padding-top:1.75rem;padding-bottom:4rem}
.fp-card{background:hsl(var(--card));border:1px solid hsl(var(--border));border-radius:1rem;padding:1.75rem;box-shadow:0 1px 3px rgba(0,0,0,.06)}
.fp-field{margin-bottom:1.25rem}
.fp-label{display:block;font-size:.8rem;font-weight:700;color:hsl(var(--muted-foreground));text-transform:uppercase;letter-spacing:.05em;margin-bottom:.45rem}
.fp-card input[type="text"],.fp-card input[type="number"],.fp-card input[type="date"],.fp-card select,.fp-card textarea,.fp-card input[type="file"]{width:100%;padding:.625rem .875rem;border:1px solid hsl(var(--border));border-radius:.5rem;background:hsl(var(--background));color:hsl(var(--foreground));font-size:.9rem;outline:none;transition:border .15s;font-family:inherit}
.fp-card input:focus,.fp-card select:focus,.fp-card textarea:focus{border-color:#fe8210;box-shadow:0 0 0 3px rgba(254,130,16,.12)}
.fp-card textarea{min-height:80px;resize:vertical}
.fp-help{font-size:.775rem;color:hsl(var(--muted-foreground));margin:.3rem 0 0}
.fp-error{font-size:.78rem;color:#dc2626;font-weight:600;margin:.3rem 0 0}
.fp-actions{display:flex;gap:.75rem;padding-top:.25rem}
.fp-btn{display:inline-flex;align-items:center;gap:.45rem;padding:.625rem 1.25rem;border-radius:.5rem;font-size:.875rem;font-weight:600;text-decoration:none;border:none;cursor:pointer;transition:all .15s;white-space:nowrap}
.fp-btn-primary{background:#fe8210;color:#fff}
.fp-btn-primary:hover{background:#e87207;color:#fff;transform:translateY(-1px);box-shadow:0 4px 14px rgba(254,130,16,.4)}
.fp-btn-outline{background:transparent;color:hsl(var(--foreground));border:1px solid hsl(var(--border))}
.fp-btn-outline:hover{background:hsl(var(--accent))}
@media(max-width:520px){.fp-container{padding:0 1rem}.fp-card{padding:1.25rem}.fp-actions{flex-direction:column}}
</style>
{% endblock %}
```

- [ ] **Step 6: Criar `forms_pcf/templates/reembolso_sucesso.html`**

```html
{% extends 'base.html' %}
{% block titulo %}Reembolso Enviado — PCF{% endblock %}

{% block content %}
<div class="fp-page">
  <div class="fp-header">
    <div class="fp-container">
      <h1 class="fp-title">Pedido Enviado!</h1>
      <p class="fp-sub">Seu pedido de reembolso foi recebido pelo ADM/FIN.</p>
    </div>
  </div>
  <div class="fp-container fp-body" style="text-align:center">
    <div class="fp-success-icon">
      <svg width="36" height="36" viewBox="0 0 24 24" fill="none" stroke="#059669" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
    </div>
    <p style="color:hsl(var(--muted-foreground));margin:.75rem 0 1.5rem;font-size:.9rem">O time de ADM/FIN foi notificado e analisará seu pedido em breve.</p>
    <a href="{% url 'inicio' %}" class="fp-btn fp-btn-primary" style="display:inline-flex">Voltar ao Início</a>
  </div>
</div>

<style>
*,*::before,*::after{box-sizing:border-box}
.fp-page{background:hsl(var(--background));min-height:100vh}
.fp-container{max-width:480px;margin:0 auto;padding:0 1.5rem}
.fp-header{background:linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%);padding:2.25rem 0 2.75rem;position:relative;overflow:hidden}
.fp-header::after{content:'';position:absolute;bottom:-2px;left:0;right:0;height:48px;background:hsl(var(--background));clip-path:ellipse(55% 100% at 50% 100%)}
.fp-title{font-size:clamp(1.25rem,3vw,1.75rem);font-weight:800;color:#fff;margin:0 0 .4rem;line-height:1.15;position:relative;z-index:1}
.fp-sub{font-size:.875rem;color:rgba(255,255,255,.55);margin:0;position:relative;z-index:1}
.fp-body{padding-top:2.5rem;padding-bottom:4rem}
.fp-success-icon{width:72px;height:72px;background:#d1fae5;border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto .5rem}
.fp-btn{display:inline-flex;align-items:center;gap:.45rem;padding:.625rem 1.25rem;border-radius:.5rem;font-size:.875rem;font-weight:600;text-decoration:none;border:none;cursor:pointer;transition:all .15s;white-space:nowrap}
.fp-btn-primary{background:#fe8210;color:#fff}
.fp-btn-primary:hover{background:#e87207;color:#fff}
</style>
{% endblock %}
```

- [ ] **Step 7: Criar `forms_pcf/templates/reembolso_inbox.html`**

```html
{% extends 'base.html' %}
{% block titulo %}Reembolsos — ADM/FIN{% endblock %}

{% block content %}
<div class="fp-page">
  <div class="fp-header">
    <div class="fp-container-wide">
      <p class="fp-breadcrumb">
        <a href="{% url 'adm:painel' %}" class="fp-breadcrumb-link">Financeiro</a>
        <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
        Reembolsos
      </p>
      <h1 class="fp-title">Pedidos de Reembolso</h1>
      <p class="fp-sub">Gerencie as solicitações de reembolso dos voluntários.</p>
    </div>
  </div>

  <div class="fp-container-wide fp-body">

    <!-- Tabs -->
    <div class="fp-tabs">
      <a href="?status=PENDENTE" class="fp-tab {% if status_ativo == 'PENDENTE' %}fp-tab-active{% endif %}">
        Pendentes <span class="fp-tab-badge">{{ contagem_pendente }}</span>
      </a>
      <a href="?status=APROVADO" class="fp-tab {% if status_ativo == 'APROVADO' %}fp-tab-active{% endif %}">
        Aprovados <span class="fp-tab-badge fp-badge-green">{{ contagem_aprovado }}</span>
      </a>
      <a href="?status=REJEITADO" class="fp-tab {% if status_ativo == 'REJEITADO' %}fp-tab-active{% endif %}">
        Rejeitados <span class="fp-tab-badge fp-badge-red">{{ contagem_rejeitado }}</span>
      </a>
    </div>

    <div class="fp-table-card">
      <table class="fp-table">
        <thead>
          <tr>
            <th>Solicitante</th>
            <th>Área</th>
            <th>Categoria</th>
            <th>Data Gasto</th>
            <th class="fp-th-right">Valor</th>
            <th>Comprovante</th>
            {% if status_ativo == 'PENDENTE' %}<th></th>{% endif %}
            {% if status_ativo == 'REJEITADO' %}<th>Motivo</th>{% endif %}
          </tr>
        </thead>
        <tbody>
          {% for p in pedidos %}
          <tr>
            <td class="fp-td-bold">{{ p.solicitante.get_full_name|default:p.solicitante.username }}</td>
            <td><span class="fp-area-tag">{{ p.solicitante.area }}</span></td>
            <td>{{ p.categoria.nome }}</td>
            <td class="fp-td-muted">{{ p.data_gasto|date:"d/m/Y" }}</td>
            <td class="fp-td-right fp-td-bold fp-color-neg">R$ {{ p.valor|floatformat:2 }}</td>
            <td>
              {% if p.comprovante %}
              <a href="{{ p.comprovante.url }}" target="_blank" class="fp-link">Ver</a>
              {% else %}—{% endif %}
            </td>
            {% if status_ativo == 'PENDENTE' %}
            <td class="fp-td-actions">
              <form method="post" action="{% url 'forms_pcf:reembolso_aprovar' p.pk %}" style="display:inline">
                {% csrf_token %}
                <button type="submit" class="fp-action-btn fp-btn-approve">Aprovar</button>
              </form>
              <button class="fp-action-btn fp-btn-reject" onclick="toggleRejeitar({{ p.pk }})">Rejeitar</button>
              <div class="fp-reject-form" id="rejeitar-{{ p.pk }}" style="display:none;margin-top:.5rem">
                <form method="post" action="{% url 'forms_pcf:reembolso_rejeitar' p.pk %}">
                  {% csrf_token %}
                  <textarea name="observacao_adm" class="fp-reject-textarea" placeholder="Motivo da rejeição (obrigatório)" required rows="2"></textarea>
                  <div style="display:flex;gap:.5rem;margin-top:.375rem">
                    <button type="submit" class="fp-action-btn fp-btn-reject">Confirmar Rejeição</button>
                    <button type="button" class="fp-action-btn fp-btn-cancel" onclick="toggleRejeitar({{ p.pk }})">Cancelar</button>
                  </div>
                </form>
              </div>
            </td>
            {% endif %}
            {% if status_ativo == 'REJEITADO' %}
            <td class="fp-td-muted fp-td-motivo">{{ p.observacao_adm|default:"—" }}</td>
            {% endif %}
          </tr>
          {% empty %}
          <tr>
            <td colspan="7">
              <div class="fp-empty">
                <div class="fp-empty-icon">
                  <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/></svg>
                </div>
                <p class="fp-empty-title">Nenhum pedido {{ status_ativo|lower }}</p>
              </div>
            </td>
          </tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
</div>

<script>
function toggleRejeitar(id) {
  var el = document.getElementById('rejeitar-' + id);
  el.style.display = el.style.display === 'none' ? 'block' : 'none';
}
</script>

<style>
*,*::before,*::after{box-sizing:border-box}
.fp-page{background:hsl(var(--background));min-height:100vh}
.fp-container-wide{max-width:1060px;margin:0 auto;padding:0 1.5rem}
.fp-header{background:linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%);padding:2.25rem 0 2.75rem;position:relative;overflow:hidden}
.fp-header::after{content:'';position:absolute;bottom:-2px;left:0;right:0;height:48px;background:hsl(var(--background));clip-path:ellipse(55% 100% at 50% 100%)}
.fp-breadcrumb{display:inline-flex;align-items:center;gap:.375rem;color:rgba(255,255,255,.45);font-size:.72rem;font-weight:600;letter-spacing:.06em;text-transform:uppercase;margin:0 0 .75rem;position:relative;z-index:1}
.fp-breadcrumb-link{color:rgba(255,255,255,.55);text-decoration:none}
.fp-breadcrumb-link:hover{color:#fff}
.fp-title{font-size:clamp(1.375rem,3vw,1.875rem);font-weight:800;color:#fff;margin:0 0 .4rem;line-height:1.15;position:relative;z-index:1}
.fp-sub{font-size:.875rem;color:rgba(255,255,255,.55);margin:0;position:relative;z-index:1}
.fp-body{padding-top:1.75rem;padding-bottom:4rem}
.fp-tabs{display:flex;gap:.5rem;margin-bottom:1rem;flex-wrap:wrap}
.fp-tab{display:inline-flex;align-items:center;gap:.5rem;padding:.5rem 1rem;border-radius:.5rem;font-size:.85rem;font-weight:600;text-decoration:none;background:hsl(var(--card));border:1px solid hsl(var(--border));color:hsl(var(--muted-foreground));transition:all .15s}
.fp-tab:hover{background:hsl(var(--accent));color:hsl(var(--foreground))}
.fp-tab-active{background:#0f172a;color:#fff;border-color:#0f172a}
.fp-tab-badge{font-size:.7rem;font-weight:700;background:hsl(var(--accent));color:hsl(var(--muted-foreground));padding:.1rem .45rem;border-radius:9999px}
.fp-tab-active .fp-tab-badge{background:rgba(255,255,255,.2);color:#fff}
.fp-badge-green{background:#d1fae5;color:#065f46}
.fp-badge-red{background:#fee2e2;color:#991b1b}
.fp-table-card{background:hsl(var(--card));border:1px solid hsl(var(--border));border-radius:1rem;overflow:hidden;box-shadow:0 1px 3px rgba(0,0,0,.06)}
.fp-table{width:100%;border-collapse:collapse;font-size:.875rem}
.fp-table thead tr{background:hsl(var(--accent));border-bottom:1px solid hsl(var(--border))}
.fp-table th{padding:.75rem 1rem;font-size:.72rem;font-weight:700;color:hsl(var(--muted-foreground));text-transform:uppercase;letter-spacing:.05em;white-space:nowrap}
.fp-table td{padding:.8rem 1rem;border-bottom:1px solid hsl(var(--border));vertical-align:top}
.fp-table tbody tr:last-child td{border-bottom:none}
.fp-table tbody tr:hover{background:hsl(var(--accent))}
.fp-th-right,.fp-td-right{text-align:right}
.fp-td-bold{font-weight:600}
.fp-td-muted{color:hsl(var(--muted-foreground));font-size:.82rem}
.fp-td-motivo{max-width:200px;word-break:break-word}
.fp-color-neg{color:#dc2626}
.fp-area-tag{font-size:.72rem;font-weight:600;background:hsl(var(--accent));color:hsl(var(--muted-foreground));padding:.2rem .55rem;border-radius:9999px;white-space:nowrap}
.fp-link{color:#0ea5e9;font-weight:600;font-size:.82rem;text-decoration:none}
.fp-link:hover{text-decoration:underline}
.fp-td-actions{white-space:nowrap;min-width:160px}
.fp-action-btn{font-size:.78rem;font-weight:600;padding:.3rem .7rem;border-radius:.375rem;border:1px solid hsl(var(--border));cursor:pointer;transition:all .12s;display:inline-block}
.fp-btn-approve{background:#d1fae5;color:#065f46;border-color:#a7f3d0}
.fp-btn-approve:hover{background:#a7f3d0}
.fp-btn-reject{background:#fee2e2;color:#991b1b;border-color:#fecaca}
.fp-btn-reject:hover{background:#fecaca}
.fp-btn-cancel{background:hsl(var(--accent));color:hsl(var(--foreground))}
.fp-reject-textarea{width:100%;padding:.5rem .75rem;border:1px solid hsl(var(--border));border-radius:.5rem;font-size:.82rem;font-family:inherit;resize:vertical;background:hsl(var(--background));color:hsl(var(--foreground));outline:none}
.fp-reject-textarea:focus{border-color:#dc2626;box-shadow:0 0 0 3px rgba(220,38,38,.1)}
.fp-empty{padding:3rem 2rem;text-align:center}
.fp-empty-icon{width:48px;height:48px;background:hsl(var(--accent));border-radius:.875rem;display:flex;align-items:center;justify-content:center;color:hsl(var(--muted-foreground));margin:0 auto .75rem}
.fp-empty-title{font-size:.9375rem;font-weight:600;color:hsl(var(--foreground));margin:0}
@media(max-width:768px){.fp-container-wide{padding:0 1rem}.fp-table{font-size:.8rem}.fp-table th,.fp-table td{padding:.65rem .75rem}}
</style>
{% endblock %}
```

- [ ] **Step 8: Rodar testes — devem passar**

```
python manage.py test forms_pcf.tests.EnviarReembolsoViewTest forms_pcf.tests.AprovarReembolsoViewTest forms_pcf.tests.RejeitarReembolsoViewTest forms_pcf.tests.ReembolsoInboxPermissionTest -v 2
```
Esperado: 9 testes passando.

- [ ] **Step 9: Commit**

```
git add forms_pcf/
git commit -m "feat(forms_pcf): reembolso — envio, inbox, aprovação/rejeição com Lancamento e e-mail"
```

---

## Task 4: Integração ADM — receptores, painel e templates

**Files:**
- Modify: `adm/views.py`, `adm/urls.py`, `adm/forms.py`, `adm/templates/painel_adm.html`
- Create: `adm/templates/receptores_reembolso.html`, `adm/templates/form_receptor.html`

**Interfaces:**
- Consumes: `ReceptorNotificacaoReembolso` de Task 1; `PedidoReembolso` de Task 3; padrão de views `adm_escrita_required` e `AdmEscritaMixin` de `adm/views.py`
- Produces: URLs `adm:receptores_reembolso`, `adm:receptor_criar`, `adm:receptor_editar`, `adm:receptor_deletar`; card Reembolsos no painel

- [ ] **Step 1: Escrever testes de receptores e painel**

Adicione ao `adm/tests.py`:

```python
from forms_pcf.models import ReceptorNotificacaoReembolso, PedidoReembolso


class ReceptoresReembolsoViewTest(TestCase):
    def _adm_client(self):
        c = Client()
        u = User.objects.create_user(username='adm_r', password='pw', area='ADM/FIN')
        c.force_login(u)
        return c, u

    def test_lista_acessivel_por_adm(self):
        c, _ = self._adm_client()
        resp = c.get(reverse('adm:receptores_reembolso'))
        self.assertEqual(resp.status_code, 200)

    def test_criar_receptor(self):
        c, _ = self._adm_client()
        resp = c.post(reverse('adm:receptor_criar'), {
            'nome': 'Financeiro PCF',
            'email': 'fin@pcf.org',
            'ativo': True,
        })
        self.assertRedirects(resp, reverse('adm:receptores_reembolso'))
        self.assertEqual(ReceptorNotificacaoReembolso.objects.count(), 1)

    def test_outro_area_recebe_403(self):
        c = Client()
        u = User.objects.create_user(username='mkt_r', password='pw', area='MARKETING')
        c.force_login(u)
        resp = c.get(reverse('adm:receptores_reembolso'))
        self.assertEqual(resp.status_code, 403)


class PainelReembolsoBadgeTest(TestCase):
    def setUp(self):
        self.client = Client()
        self.adm = User.objects.create_user(username='adm_badge', password='pw', area='ADM/FIN')
        self.client.force_login(self.adm)
        cat = Categoria.objects.create(nome='Geral', tipo='DESPESA')
        PedidoReembolso.objects.create(
            solicitante=self.adm, valor='10.00',
            descricao='x', data_gasto=timezone.now().date(),
            categoria=cat, comprovante='reembolsos/x.jpg', status='PENDENTE'
        )

    def test_painel_contem_contagem_pendente(self):
        resp = self.client.get(reverse('adm:painel'))
        self.assertEqual(resp.status_code, 200)
        self.assertIn('reembolsos_pendentes', resp.context)
        self.assertEqual(resp.context['reembolsos_pendentes'], 1)
```

- [ ] **Step 2: Rodar — devem falhar**

```
python manage.py test adm.tests.ReceptoresReembolsoViewTest adm.tests.PainelReembolsoBadgeTest -v 2
```
Esperado: `NoReverseMatch` para `adm:receptores_reembolso` e `KeyError` para `reembolsos_pendentes`.

- [ ] **Step 3: Adicionar `ReceptorNotificacaoReembolsoForm` em `adm/forms.py`**

Acrescente ao final do arquivo:

```python
from forms_pcf.models import ReceptorNotificacaoReembolso


class ReceptorNotificacaoReembolsoForm(forms.ModelForm):
    class Meta:
        model = ReceptorNotificacaoReembolso
        fields = ['nome', 'email', 'ativo']
        widgets = {
            'nome': forms.TextInput(attrs={'class': 'form-control', 'placeholder': 'Nome do receptor'}),
            'email': forms.EmailInput(attrs={'class': 'form-control', 'placeholder': 'email@exemplo.com'}),
            'ativo': forms.CheckboxInput(attrs={'class': 'form-check-input'}),
        }
        labels = {
            'nome': 'Nome',
            'email': 'E-mail',
            'ativo': 'Ativo',
        }
```

- [ ] **Step 4: Adicionar views de receptores em `adm/views.py`**

Adicione os imports necessários no topo (se não existirem):
```python
from forms_pcf.models import ReceptorNotificacaoReembolso, PedidoReembolso
from .forms import CategoriaForm, LancamentoForm, ReceptorNotificacaoReembolsoForm
```

Acrescente ao final de `adm/views.py`:

```python
@adm_escrita_required
def receptores_reembolso(request):
    receptores = ReceptorNotificacaoReembolso.objects.all()
    return render(request, 'receptores_reembolso.html', {'receptores': receptores})


@adm_escrita_required
def receptor_criar(request):
    form = ReceptorNotificacaoReembolsoForm(request.POST or None)
    if form.is_valid():
        form.save()
        messages.success(request, 'Receptor adicionado!')
        return redirect('adm:receptores_reembolso')
    return render(request, 'form_receptor.html', {'form': form, 'titulo': 'Novo Receptor'})


@adm_escrita_required
def receptor_editar(request, pk):
    receptor = get_object_or_404(ReceptorNotificacaoReembolso, pk=pk)
    form = ReceptorNotificacaoReembolsoForm(request.POST or None, instance=receptor)
    if form.is_valid():
        form.save()
        messages.success(request, 'Receptor atualizado!')
        return redirect('adm:receptores_reembolso')
    return render(request, 'form_receptor.html', {'form': form, 'titulo': 'Editar Receptor', 'objeto': receptor})


@adm_escrita_required
def receptor_deletar(request, pk):
    receptor = get_object_or_404(ReceptorNotificacaoReembolso, pk=pk)
    if request.method == 'POST':
        receptor.delete()
        messages.success(request, 'Receptor removido.')
        return redirect('adm:receptores_reembolso')
    return render(request, 'form_receptor.html', {
        'objeto': receptor, 'confirmar_delecao': True, 'titulo': 'Remover Receptor'
    })
```

Também atualize a view `painel` para incluir a contagem de reembolsos pendentes:

```python
@adm_acesso_required
def painel(request):
    total_receitas = Lancamento.objects.filter(tipo='RECEITA').aggregate(t=Sum('valor'))['t'] or Decimal('0')
    total_despesas = Lancamento.objects.filter(tipo='DESPESA').aggregate(t=Sum('valor'))['t'] or Decimal('0')
    saldo = total_receitas - total_despesas
    ultimos = Lancamento.objects.select_related('categoria').order_by('-data', '-criado_em')[:10]
    reembolsos_pendentes = PedidoReembolso.objects.filter(status='PENDENTE').count()

    return render(request, 'painel_adm.html', {
        'saldo': saldo,
        'total_receitas': total_receitas,
        'total_despesas': total_despesas,
        'ultimos': ultimos,
        'reembolsos_pendentes': reembolsos_pendentes,
    })
```

- [ ] **Step 5: Adicionar URLs em `adm/urls.py`**

Acrescente ao final de `urlpatterns`:

```python
    path('notificacoes-reembolso/', views.receptores_reembolso, name='receptores_reembolso'),
    path('notificacoes-reembolso/novo/', views.receptor_criar, name='receptor_criar'),
    path('notificacoes-reembolso/<int:pk>/editar/', views.receptor_editar, name='receptor_editar'),
    path('notificacoes-reembolso/<int:pk>/deletar/', views.receptor_deletar, name='receptor_deletar'),
```

- [ ] **Step 6: Criar `adm/templates/receptores_reembolso.html`**

```html
{% extends 'base.html' %}
{% block titulo %}Receptores de Notificação — Financeiro{% endblock %}

{% block content %}
<div class="fin-page">
  <div class="fin-header">
    <div class="fin-container">
      <div class="fin-header-inner">
        <div class="fin-header-text">
          <p class="fin-breadcrumb">
            <a href="{% url 'adm:painel' %}" class="fin-breadcrumb-link">Financeiro</a>
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
            Receptores de Notificação
          </p>
          <h1 class="fin-title">Receptores de E-mail</h1>
          <p class="fin-sub">Gerencie quem recebe notificações de novos pedidos de reembolso.</p>
        </div>
        <a href="{% url 'adm:receptor_criar' %}" class="fin-btn fin-btn-primary">
          <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
          Novo Receptor
        </a>
      </div>
    </div>
  </div>

  <div class="fin-container fin-body">
    <div class="fin-table-card">
      <table class="fin-table">
        <thead>
          <tr><th>Nome</th><th>E-mail</th><th>Status</th><th></th></tr>
        </thead>
        <tbody>
          {% for r in receptores %}
          <tr>
            <td class="fin-td-name">{{ r.nome }}</td>
            <td class="fin-td-muted">{{ r.email }}</td>
            <td>
              {% if r.ativo %}
              <span class="fin-status-badge fin-status-on">Ativo</span>
              {% else %}
              <span class="fin-status-badge fin-status-off">Inativo</span>
              {% endif %}
            </td>
            <td class="fin-td-actions">
              <a href="{% url 'adm:receptor_editar' r.pk %}" class="fin-action-link">Editar</a>
              <a href="{% url 'adm:receptor_deletar' r.pk %}" class="fin-action-link fin-action-del">Remover</a>
            </td>
          </tr>
          {% empty %}
          <tr><td colspan="4">
            <div class="fin-table-empty">
              <div class="fin-empty-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
              </div>
              <p class="fin-empty-title">Nenhum receptor cadastrado</p>
              <p class="fin-empty-sub">Adicione e-mails para receber notificações de reembolso.</p>
            </div>
          </td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
</div>

<style>
*,*::before,*::after{box-sizing:border-box}
.fin-page{background:hsl(var(--background));min-height:100vh}
.fin-container{max-width:800px;margin:0 auto;padding:0 1.5rem}
.fin-header{background:linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%);padding:2.25rem 0 2.75rem;position:relative;overflow:hidden}
.fin-header::after{content:'';position:absolute;bottom:-2px;left:0;right:0;height:48px;background:hsl(var(--background));clip-path:ellipse(55% 100% at 50% 100%)}
.fin-header-inner{position:relative;z-index:1;display:flex;align-items:flex-start;justify-content:space-between;gap:1.5rem;flex-wrap:wrap}
.fin-breadcrumb{display:inline-flex;align-items:center;gap:.375rem;color:rgba(255,255,255,.45);font-size:.72rem;font-weight:600;letter-spacing:.06em;text-transform:uppercase;margin:0 0 .75rem}
.fin-breadcrumb-link{color:rgba(255,255,255,.55);text-decoration:none}
.fin-breadcrumb-link:hover{color:#fff}
.fin-title{font-size:clamp(1.375rem,3vw,1.875rem);font-weight:800;color:#fff;margin:0 0 .4rem;line-height:1.15}
.fin-sub{font-size:.875rem;color:rgba(255,255,255,.55);margin:0}
.fin-btn{display:inline-flex;align-items:center;gap:.45rem;padding:.6rem 1.2rem;border-radius:.5rem;font-size:.875rem;font-weight:600;text-decoration:none;border:none;cursor:pointer;transition:all .15s;white-space:nowrap;flex-shrink:0;position:relative;z-index:1}
.fin-btn-primary{background:#fe8210;color:#fff}
.fin-btn-primary:hover{background:#e87207;color:#fff;transform:translateY(-1px);box-shadow:0 4px 14px rgba(254,130,16,.4)}
.fin-body{padding-top:1.75rem;padding-bottom:4rem}
.fin-table-card{background:hsl(var(--card));border:1px solid hsl(var(--border));border-radius:1rem;overflow:hidden;box-shadow:0 1px 3px rgba(0,0,0,.06)}
.fin-table{width:100%;border-collapse:collapse;font-size:.875rem}
.fin-table thead tr{background:hsl(var(--accent));border-bottom:1px solid hsl(var(--border))}
.fin-table th{padding:.75rem 1.25rem;font-size:.72rem;font-weight:700;color:hsl(var(--muted-foreground));text-transform:uppercase;letter-spacing:.05em}
.fin-table td{padding:.875rem 1.25rem;border-bottom:1px solid hsl(var(--border));vertical-align:middle}
.fin-table tbody tr:last-child td{border-bottom:none}
.fin-table tbody tr:hover{background:hsl(var(--accent))}
.fin-td-name{font-weight:600}
.fin-td-muted{color:hsl(var(--muted-foreground));font-size:.875rem}
.fin-td-actions{text-align:right;white-space:nowrap}
.fin-status-badge{display:inline-block;font-size:.72rem;font-weight:700;padding:.25rem .625rem;border-radius:9999px}
.fin-status-on{background:#f0fdf4;color:#166534;border:1px solid #bbf7d0}
.fin-status-off{background:#f8fafc;color:#94a3b8;border:1px solid #e2e8f0}
.fin-action-link{font-size:.78rem;font-weight:600;padding:.25rem .625rem;border-radius:.375rem;text-decoration:none;border:1px solid hsl(var(--border));color:hsl(var(--foreground));background:transparent;transition:all .12s;margin-left:.25rem;display:inline-block}
.fin-action-link:hover{background:hsl(var(--accent))}
.fin-action-del{color:#dc2626;border-color:#fecaca}
.fin-action-del:hover{background:#fee2e2}
.fin-table-empty{padding:3rem 2rem;text-align:center}
.fin-empty-icon{width:48px;height:48px;background:hsl(var(--accent));border-radius:.875rem;display:flex;align-items:center;justify-content:center;color:hsl(var(--muted-foreground));margin:0 auto .75rem}
.fin-empty-title{font-size:.9375rem;font-weight:600;color:hsl(var(--foreground));margin:0 0 .35rem}
.fin-empty-sub{font-size:.825rem;color:hsl(var(--muted-foreground));margin:0}
@media(max-width:768px){.fin-container{padding:0 1rem}.fin-header-inner{flex-direction:column;gap:1rem}.fin-btn-primary{width:100%;justify-content:center}}
</style>
{% endblock %}
```

- [ ] **Step 7: Criar `adm/templates/form_receptor.html`**

```html
{% extends 'base.html' %}
{% block titulo %}{{ titulo }} — Financeiro{% endblock %}

{% block content %}
<div class="fin-page">
  <div class="fin-header">
    <div class="fin-container">
      <div class="fin-header-inner">
        <div class="fin-header-text">
          <p class="fin-breadcrumb">
            <a href="{% url 'adm:painel' %}" class="fin-breadcrumb-link">Financeiro</a>
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
            <a href="{% url 'adm:receptores_reembolso' %}" class="fin-breadcrumb-link">Receptores</a>
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="m9 18 6-6-6-6"/></svg>
            {{ titulo }}
          </p>
          <h1 class="fin-title">{{ titulo }}</h1>
        </div>
      </div>
    </div>
  </div>

  <div class="fin-container fin-body">

    {% if confirmar_delecao %}
    <div class="fin-confirm-card">
      <div class="fin-confirm-icon">
        <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 14H6L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4h6v2"/></svg>
      </div>
      <div class="fin-confirm-text">
        <p class="fin-confirm-title">Remover receptor?</p>
        <p class="fin-confirm-sub"><strong>{{ objeto.nome }}</strong> ({{ objeto.email }}) não receberá mais notificações de reembolso.</p>
      </div>
    </div>
    <form method="post" class="fin-confirm-actions">
      {% csrf_token %}
      <button type="submit" class="fin-btn fin-btn-danger">Sim, remover</button>
      <a href="{% url 'adm:receptores_reembolso' %}" class="fin-btn fin-btn-outline">Cancelar</a>
    </form>

    {% else %}
    <div class="fin-form-card">
      <form method="post">
        {% csrf_token %}
        <div class="fin-field">
          <label class="fin-label">{{ form.nome.label }}</label>
          {{ form.nome }}
          {% if form.nome.errors %}<p class="fin-error">{{ form.nome.errors|join:", " }}</p>{% endif %}
        </div>
        <div class="fin-field">
          <label class="fin-label">{{ form.email.label }}</label>
          {{ form.email }}
          {% if form.email.errors %}<p class="fin-error">{{ form.email.errors|join:", " }}</p>{% endif %}
        </div>
        <div class="fin-field fin-field-check">
          {{ form.ativo }}
          <label class="fin-label-check">{{ form.ativo.label }}</label>
        </div>
        <div class="fin-form-actions">
          <button type="submit" class="fin-btn fin-btn-primary">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="20 6 9 17 4 12"/></svg>
            Salvar
          </button>
          <a href="{% url 'adm:receptores_reembolso' %}" class="fin-btn fin-btn-outline">Cancelar</a>
        </div>
      </form>
    </div>
    {% endif %}

  </div>
</div>

<style>
*,*::before,*::after{box-sizing:border-box}
.fin-page{background:hsl(var(--background));min-height:100vh}
.fin-container{max-width:480px;margin:0 auto;padding:0 1.5rem}
.fin-header{background:linear-gradient(135deg,#0f172a 0%,#1e293b 55%,#0f3460 100%);padding:2.25rem 0 2.75rem;position:relative;overflow:hidden}
.fin-header::after{content:'';position:absolute;bottom:-2px;left:0;right:0;height:48px;background:hsl(var(--background));clip-path:ellipse(55% 100% at 50% 100%)}
.fin-header-inner{position:relative;z-index:1}
.fin-breadcrumb{display:inline-flex;align-items:center;gap:.375rem;color:rgba(255,255,255,.45);font-size:.72rem;font-weight:600;letter-spacing:.06em;text-transform:uppercase;margin:0 0 .75rem}
.fin-breadcrumb-link{color:rgba(255,255,255,.55);text-decoration:none}
.fin-breadcrumb-link:hover{color:#fff}
.fin-title{font-size:clamp(1.25rem,3vw,1.625rem);font-weight:800;color:#fff;margin:0;line-height:1.15}
.fin-btn{display:inline-flex;align-items:center;gap:.45rem;padding:.625rem 1.25rem;border-radius:.5rem;font-size:.875rem;font-weight:600;text-decoration:none;border:none;cursor:pointer;transition:all .15s;white-space:nowrap}
.fin-btn-primary{background:#fe8210;color:#fff}
.fin-btn-primary:hover{background:#e87207;color:#fff;transform:translateY(-1px);box-shadow:0 4px 14px rgba(254,130,16,.4)}
.fin-btn-outline{background:transparent;color:hsl(var(--foreground));border:1px solid hsl(var(--border))}
.fin-btn-outline:hover{background:hsl(var(--accent))}
.fin-btn-danger{background:#dc2626;color:#fff}
.fin-btn-danger:hover{background:#b91c1c;color:#fff}
.fin-body{padding-top:1.75rem;padding-bottom:4rem}
.fin-form-card{background:hsl(var(--card));border:1px solid hsl(var(--border));border-radius:1rem;padding:1.75rem;box-shadow:0 1px 3px rgba(0,0,0,.06)}
.fin-field{margin-bottom:1.25rem}
.fin-label{display:block;font-size:.8rem;font-weight:700;color:hsl(var(--muted-foreground));text-transform:uppercase;letter-spacing:.05em;margin-bottom:.45rem}
.fin-form-card input[type="text"],.fin-form-card input[type="email"]{width:100%;padding:.625rem .875rem;border:1px solid hsl(var(--border));border-radius:.5rem;background:hsl(var(--background));color:hsl(var(--foreground));font-size:.9rem;outline:none;transition:border .15s;font-family:inherit}
.fin-form-card input:focus{border-color:#fe8210;box-shadow:0 0 0 3px rgba(254,130,16,.12)}
.fin-error{font-size:.78rem;color:#dc2626;font-weight:600;margin:.3rem 0 0}
.fin-field-check{display:flex;align-items:center;gap:.625rem;margin-bottom:1.5rem}
.fin-label-check{font-size:.875rem;font-weight:600;color:hsl(var(--foreground));margin:0;cursor:pointer}
.fin-form-card input[type="checkbox"]{width:17px;height:17px;border-radius:.25rem;cursor:pointer;accent-color:#fe8210}
.fin-form-actions{display:flex;gap:.75rem}
.fin-confirm-card{background:#fff7ed;border:1px solid #fed7aa;border-radius:1rem;padding:1.5rem;display:flex;gap:1rem;align-items:flex-start;margin-bottom:1.25rem}
.fin-confirm-icon{width:44px;height:44px;background:#fee2e2;color:#dc2626;border-radius:.75rem;display:flex;align-items:center;justify-content:center;flex-shrink:0}
.fin-confirm-title{font-size:1rem;font-weight:700;color:#9a3412;margin:0 0 .35rem}
.fin-confirm-sub{font-size:.875rem;color:#c2410c;margin:0;line-height:1.5}
.fin-confirm-actions{display:flex;gap:.75rem}
@media(max-width:520px){.fin-container{padding:0 1rem}.fin-form-card{padding:1.25rem}.fin-form-actions{flex-direction:column}.fin-confirm-card{flex-direction:column;gap:.75rem}}
</style>
{% endblock %}
```

- [ ] **Step 8: Adicionar card Reembolsos no `adm/templates/painel_adm.html`**

Localize o trecho das 4 tool cards (a linha que termina com `</div>` encerrando `fin-tools-grid`) e adicione o 5º card **antes** do `</div>` que fecha `fin-tools-grid`:

```html
        <a href="{% url 'forms_pcf:reembolso_inbox' %}" class="fin-tool-card">
          <div class="fin-tool-icon fin-tool-blue">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/><polyline points="14 2 14 8 20 8"/><line x1="16" y1="13" x2="8" y2="13"/><line x1="16" y1="17" x2="8" y2="17"/><polyline points="10 9 9 9 8 9"/></svg>
          </div>
          <div class="fin-tool-body">
            <div class="fin-tool-title">
              Reembolsos
              {% if reembolsos_pendentes %}
              <span class="fin-tool-pending-badge">{{ reembolsos_pendentes }}</span>
              {% endif %}
            </div>
            <div class="fin-tool-desc">Aprovar ou rejeitar pedidos de reembolso dos voluntários</div>
          </div>
          <svg class="fin-tool-arrow" width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14"/><path d="m12 5 7 7-7 7"/></svg>
        </a>
```

No bloco `<style>` do `painel_adm.html`, adicione antes do `</style>`:

```css
.fin-tool-blue{background:#e0f2fe;color:#0284c7}
.fin-tool-pending-badge{display:inline-flex;align-items:center;justify-content:center;min-width:18px;height:18px;padding:0 5px;background:#dc2626;color:#fff;font-size:.65rem;font-weight:800;border-radius:9999px;margin-left:.375rem;vertical-align:middle}
```

- [ ] **Step 9: Rodar todos os testes**

```
python manage.py test forms_pcf adm -v 2
```
Esperado: todos os testes passando (mínimo 22 testes).

- [ ] **Step 10: Commit**

```
git add adm/ forms_pcf/
git commit -m "feat(adm): receptores de reembolso, card no painel e integração forms_pcf"
```

---

## Self-Review

**Spec coverage:**
- ✅ `FeedbackArea`, `PedidoReembolso`, `ReceptorNotificacaoReembolso` — Task 1
- ✅ `ORIGEM_CHOICES` com REEMBOLSO — Task 1
- ✅ Feedback anônimo (sem criado_por) — Task 2
- ✅ Permissões feedback inbox (PROJETOS+TRIADE+super) — Task 2
- ✅ Reembolso com comprovante obrigatório — Task 3
- ✅ E-mail só para receptores ativos — Task 3
- ✅ Aprovação cria Lancamento com origem=REEMBOLSO — Task 3
- ✅ Rejeição exige motivo — Task 3
- ✅ Inbox com tabs por status + contagens — Task 3
- ✅ Views de receptores (CRUD) em /adm/ — Task 4
- ✅ Card Reembolsos no painel com badge de pendentes — Task 4
- ✅ Permissões reembolso (ADM/FIN+super) — Tasks 3 e 4
- ✅ Templates com PCF design system (fp- prefix) — Tasks 2, 3, 4
- ✅ Login obrigatório em todas as URLs — todas as views usam LoginRequiredMixin

**Placeholders:** nenhum.

**Consistência de tipos:** `PedidoReembolso.status='PENDENTE'` usado em Task 1 (model), Task 3 (views), Task 4 (testes) — consistente. `origem='REEMBOLSO'` definido em Task 1 e usado em Task 3 `AprovarReembolsoView` — consistente.

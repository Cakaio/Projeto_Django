# PWA + Notificações Push — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tornar o PCF instalável como app no celular (Android e iOS) com notificações push, sem loja de aplicativos e sem build nativo.

**Architecture:** Uma PWA servida pelo próprio Django — `manifest.webmanifest` + service worker em `/sw.js` + ícones gerados do logo. As notificações usam Web Push padrão (VAPID, `pywebpush`), com uma tabela de inscrições por aparelho e uma função de envio única (`enviar_push`) consumida por quatro gatilhos. Nada de API REST nova: as views de push falam JSON, o resto do sistema continua server-side como está.

**Tech Stack:** Django 4.2.27, `pywebpush` 2.4.0 (traz `py-vapid`, `http-ece`, `cryptography`), Pillow 11.3.0 (já instalado), WhiteNoise, JavaScript sem framework.

**Spec:** `docs/superpowers/specs/2026-08-16-pwa-push-design.md`

## Global Constraints

- **Python 3.10+ obrigatório** — `pywebpush` 2.4.0 exige. O `Dockerfile` usa `python:3.11-slim` ✅. A web app do PythonAnywhere **precisa ser verificada** (Passo 0 do spec).
- **Idioma:** todo código, comentário, nome de variável, template e texto de UI em **português do Brasil**. É a convenção do projeto inteiro.
- **`timezone.now()`, nunca `datetime.now()`** — `USE_TZ = True`.
- **Voluntário ativo é `Voluntario.objects.ativos()`** — manager já existente em `voluntario/models.py`, filtra `data_saida__isnull=True, is_active=True`. **Não** reescrever esse filtro à mão.
- **`LISTA_AREAS` importa de `voluntario.models`** — nunca copiar a tupla. Ela já foi duplicada demais neste projeto.
- **Push soma ao e-mail, não substitui.** Nenhum `send_mail` existente pode ser removido ou condicionado.
- **`webpush` sempre mockado em teste.** Nenhum teste toca a rede.
- **Comando de teste canônico:** `docker compose run --rm web python manage.py test notificacoes`
  (o ambiente de desenvolvimento definido do projeto é o `docker-compose.yml`; ver "Pré-requisito de ambiente" abaixo).
- **Sem `ImportExportModelAdmin` no app `notificacoes`** — exportar chave de push para planilha é vazamento de credencial. É a única exceção deliberada ao padrão do projeto.
- **Toda URL enviada em push é absoluta a partir da raiz** (`/voluntario/...`), porque quem consome é o service worker, não um template.

## Pré-requisito de ambiente (resolver antes da Task 1)

Nesta máquina, hoje, **a suíte de testes não roda**:

| Item | Estado | Efeito |
|---|---|---|
| Django no Python do host (3.13.3) | **ausente** | `manage.py` não executa fora do Docker |
| Engine do Docker Desktop | **parada** | `docker compose` falha ao conectar |
| Arquivo `.env` | **ausente** | `settings.py` faz `config("SECRET_KEY")` sem default → `UndefinedValueError` na importação |

Pillow **está** instalado no Python do host, então a Task 1 (ícones) é a única que roda sem resolver nada disso.

**Resolver antes de começar:** subir o Docker Desktop e criar o `.env` a partir do `.env.example` (as credenciais de banco do `.env.example` já batem com as do `docker-compose.yml`: `pcf_db` / `pcf_user` / `pcf_pass`). Sem isso, os passos "rodar o teste" deste plano não podem ser cumpridos, e **não é aceitável marcá-los como feitos sem executar**.

---

# FASE 1 — PWA instalável

Entrega valor sozinha: ícone na tela inicial e abertura em tela cheia, sem nenhum push.

---

### Task 1: Geração dos ícones

**Files:**
- Create: `notificacoes/__init__.py`, `notificacoes/management/__init__.py`, `notificacoes/management/commands/__init__.py`
- Create: `notificacoes/management/commands/gerar_icones_pwa.py`
- Create (gerado): `static/images/icons/*.png`

**Interfaces:**
- Consumes: `static/images/Logo_PCF.png` (246×248, RGBA)
- Produces: `icon-192.png`, `icon-512.png`, `icon-512-maskable.png`, `apple-touch-icon-180.png`, `badge-72.png` em `static/images/icons/`

**Atenção — o logo é pequeno.** A origem tem 246×248px, então o ícone de 512 é **upscale**. Usar `Image.LANCZOS` para minimizar o borrão. Ícone de app é visto pequeno, então é aceitável — mas se existir um logo em resolução maior (SVG ou PNG ≥512), usar ele e pular a ampliação. Registrar isso no output do comando.

- [ ] **Step 1: Criar o esqueleto do app**

```bash
mkdir -p notificacoes/management/commands
touch notificacoes/__init__.py notificacoes/management/__init__.py notificacoes/management/commands/__init__.py
```

- [ ] **Step 2: Escrever o comando**

Create `notificacoes/management/commands/gerar_icones_pwa.py`:

```python
"""Gera os ícones da PWA a partir do logo do projeto.

Roda uma vez (e de novo se o logo mudar). Os arquivos gerados entram no git:
são estáticos servidos pelo WhiteNoise, não artefato de build.
"""
from pathlib import Path

from django.conf import settings
from django.core.management.base import BaseCommand, CommandError
from PIL import Image

# Laranja da marca, o mesmo do theme-color do base.html.
COR_FUNDO = (232, 86, 15, 255)


class Command(BaseCommand):
    help = "Gera os ícones da PWA em static/images/icons/ a partir do Logo_PCF.png"

    def handle(self, *args, **options):
        origem = Path(settings.BASE_DIR) / "static" / "images" / "Logo_PCF.png"
        if not origem.exists():
            raise CommandError(f"Logo não encontrado em {origem}")

        destino = Path(settings.BASE_DIR) / "static" / "images" / "icons"
        destino.mkdir(parents=True, exist_ok=True)

        logo = Image.open(origem).convert("RGBA")
        if max(logo.size) < 512:
            self.stdout.write(self.style.WARNING(
                f"Logo tem {logo.size[0]}x{logo.size[1]}px — os ícones de 512 são "
                f"ampliados e podem sair levemente borrados. Se houver um logo "
                f"maior, substitua {origem.name} e rode este comando de novo."
            ))

        self._quadrado_transparente(logo, 192, destino / "icon-192.png")
        self._quadrado_transparente(logo, 512, destino / "icon-512.png")
        self._maskable(logo, 512, destino / "icon-512-maskable.png")
        self._fundo_opaco(logo, 180, destino / "apple-touch-icon-180.png")
        self._badge(logo, 72, destino / "badge-72.png")

        self.stdout.write(self.style.SUCCESS(f"5 ícones gerados em {destino}"))

    # ── helpers ──

    def _ajustar(self, logo, lado):
        """Redimensiona o logo cabendo num quadrado de `lado`, preservando proporção."""
        copia = logo.copy()
        copia.thumbnail((lado, lado), Image.LANCZOS)
        return copia

    def _colar_centralizado(self, base, arte):
        x = (base.width - arte.width) // 2
        y = (base.height - arte.height) // 2
        base.paste(arte, (x, y), arte)
        return base

    def _quadrado_transparente(self, logo, lado, caminho):
        base = Image.new("RGBA", (lado, lado), (0, 0, 0, 0))
        self._colar_centralizado(base, self._ajustar(logo, lado)).save(caminho, "PNG")

    def _maskable(self, logo, lado, caminho):
        """Android recorta em círculo/squircle conforme o fabricante.

        A arte fica em 80% do canvas para o recorte não decapitar o logo.
        """
        base = Image.new("RGBA", (lado, lado), COR_FUNDO)
        self._colar_centralizado(base, self._ajustar(logo, int(lado * 0.8))).save(caminho, "PNG")

    def _fundo_opaco(self, logo, lado, caminho):
        """iOS ignora transparência e renderiza o vazio como preto."""
        base = Image.new("RGBA", (lado, lado), (255, 255, 255, 255))
        self._colar_centralizado(base, self._ajustar(logo, int(lado * 0.85)))
        base.convert("RGB").save(caminho, "PNG")

    def _badge(self, logo, lado, caminho):
        """Ícone monocromático da status bar do Android: silhueta branca."""
        arte = self._ajustar(logo, lado)
        branco = Image.new("RGBA", arte.size, (255, 255, 255, 255))
        branco.putalpha(arte.getchannel("A"))
        base = Image.new("RGBA", (lado, lado), (0, 0, 0, 0))
        self._colar_centralizado(base, branco).save(caminho, "PNG")
```

- [ ] **Step 3: Registrar o app no `INSTALLED_APPS`**

Modify `TESTE/settings.py` — adicionar `'notificacoes',` ao fim da lista de apps do projeto (depois de `'projetos',`).

- [ ] **Step 4: Rodar o comando e conferir a saída**

Run: `docker compose run --rm web python manage.py gerar_icones_pwa`
Expected: `5 ícones gerados em ...`, mais o aviso de logo pequeno.

- [ ] **Step 5: Verificar os arquivos**

Run: `ls -la static/images/icons/`
Expected: 5 PNGs. Abrir `apple-touch-icon-180.png` e confirmar **fundo branco, não transparente** — é o erro mais comum aqui.

---

### Task 2: Manifest e meta tags do iOS

**Files:**
- Create: `static/manifest.webmanifest`
- Modify: `templates/base.html` (no `<head>`, junto do `apple-touch-icon` da linha 12)
- Create: `notificacoes/tests.py`

**Interfaces:**
- Consumes: os ícones da Task 1
- Produces: manifest servido em `/static/manifest.webmanifest`, com `start_url = /inicio/` e `scope = /`

- [ ] **Step 1: Escrever o teste que falha**

Create `notificacoes/tests.py`:

```python
import json

from django.contrib.staticfiles import finders
from django.test import TestCase


class ManifestTest(TestCase):
    """O manifest é arquivo estático: o test client NÃO o serve (DEBUG=False na
    suíte e o handler do staticfiles não é montado). Por isso lemos do disco."""

    def setUp(self):
        caminho = finders.find("manifest.webmanifest")
        self.assertIsNotNone(caminho, "manifest.webmanifest não encontrado nos statics")
        with open(caminho, encoding="utf-8") as f:
            self.manifest = json.load(f)

    def test_manifest_e_json_valido_com_campos_obrigatorios(self):
        self.assertEqual(self.manifest["start_url"], "/inicio/")
        self.assertEqual(self.manifest["scope"], "/")
        self.assertEqual(self.manifest["display"], "standalone")

    def test_manifest_tem_icones_192_e_512(self):
        tamanhos = {i["sizes"] for i in self.manifest["icons"]}
        self.assertIn("192x192", tamanhos)
        self.assertIn("512x512", tamanhos)

    def test_manifest_tem_icone_maskable(self):
        """Sem maskable o Android recorta o ícone e decapita o logo."""
        propositos = {i.get("purpose") for i in self.manifest["icons"]}
        self.assertIn("maskable", propositos)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.ManifestTest -v2`
Expected: FAIL — `manifest.webmanifest não encontrado nos statics`

- [ ] **Step 3: Criar o manifest**

Create `static/manifest.webmanifest`:

```json
{
  "id": "/",
  "name": "Projeto Criança Feliz",
  "short_name": "PCF",
  "description": "Gestão do Projeto Criança Feliz",
  "start_url": "/inicio/",
  "scope": "/",
  "display": "standalone",
  "orientation": "portrait",
  "background_color": "#ffffff",
  "theme_color": "#e8560f",
  "lang": "pt-BR",
  "dir": "ltr",
  "icons": [
    {"src": "/static/images/icons/icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any"},
    {"src": "/static/images/icons/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any"},
    {"src": "/static/images/icons/icon-512-maskable.png", "sizes": "512x512", "type": "image/png", "purpose": "maskable"}
  ]
}
```

Caminhos literais `/static/...` são seguros: o `CompressedStaticFilesStorage` não põe hash no nome.

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.ManifestTest -v2`
Expected: PASS (3 testes)

- [ ] **Step 5: Ligar o manifest e as meta tags do iOS no `base.html`**

Modify `templates/base.html` — substituir a linha 12 (`<link rel="apple-touch-icon" href="{% static 'images/Logo_PCF.png' %}">`) por:

```html
    <link rel="apple-touch-icon" sizes="180x180" href="{% static 'images/icons/apple-touch-icon-180.png' %}">

    <!-- PWA: permite instalar na tela de início e abrir em tela cheia. -->
    <link rel="manifest" href="{% static 'manifest.webmanifest' %}">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="PCF">
```

`apple-mobile-web-app-capable` é formalmente obsoleto em favor do `display` do manifest, mas versões de iOS ainda em uso o respeitam — manter os dois.

- [ ] **Step 6: Conferir no navegador**

Run: `docker compose up -d` e abrir `http://localhost:8000/inicio/`
Expected: DevTools → Application → Manifest mostra "Projeto Criança Feliz", sem erro de ícone.

---

### Task 3: Service worker

**Files:**
- Create: `templates/sw.js`
- Modify: `TESTE/urls.py`
- Modify: `templates/base.html` (fim do `<body>`, junto do `pcf-fx.js` da linha 128)
- Modify: `notificacoes/tests.py`

**Interfaces:**
- Produces: rota nomeada `service_worker` em `/sw.js`, servida como `application/javascript`

**Por que na raiz:** um service worker só controla páginas **abaixo do próprio caminho**. Servido de `/static/js/sw.js` o escopo seria `/static/js/` e a PWA não instalaria.

- [ ] **Step 1: Escrever o teste que falha**

Append to `notificacoes/tests.py`:

```python
class ServiceWorkerTest(TestCase):
    def test_sw_servido_na_raiz_como_javascript(self):
        """Precisa ser /sw.js e não /static/js/sw.js: o escopo de um service
        worker é limitado ao próprio caminho."""
        resposta = self.client.get("/sw.js")
        self.assertEqual(resposta.status_code, 200)
        self.assertIn("javascript", resposta["Content-Type"])

    def test_sw_nao_exige_login(self):
        """O navegador busca o sw.js sem cookie de sessão em alguns momentos."""
        resposta = self.client.get("/sw.js")
        self.assertEqual(resposta.status_code, 200)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.ServiceWorkerTest -v2`
Expected: FAIL — 404

- [ ] **Step 3: Criar o service worker**

Create `templates/sw.js`:

```javascript
/* Service worker do PCF.
 *
 * Escopo deliberadamente pequeno: recebe push, abre a tela certa no clique, e
 * mostra uma página de "sem conexão" quando a navegação falha.
 *
 * NÃO cacheia HTML. Página autenticada do Django em cache significa token CSRF
 * vencido (formulário quebra com 403) e dado de outro usuário no mesmo aparelho.
 *
 * Ao alterar este arquivo, BUMPAR a constante VERSAO — senão o navegador
 * continua servindo o service worker antigo.
 */
const VERSAO = 'pcf-v1';
const CACHE_ESTATICO = `${VERSAO}-estatico`;
const PAGINA_OFFLINE = '/notificacoes/offline/';

self.addEventListener('install', (e) => {
  e.waitUntil(
    caches.open(CACHE_ESTATICO)
      .then((c) => c.add(PAGINA_OFFLINE))
      .catch(() => {})            // sem rede na instalação não pode travar o install
      .then(() => self.skipWaiting())
  );
});

self.addEventListener('activate', (e) => {
  e.waitUntil(
    caches.keys()
      .then((nomes) => Promise.all(
        nomes.filter((n) => !n.startsWith(VERSAO)).map((n) => caches.delete(n))
      ))
      .then(() => self.clients.claim())
  );
});

self.addEventListener('fetch', (e) => {
  const req = e.request;
  if (req.method !== 'GET') return;

  const url = new URL(req.url);
  if (url.origin !== self.location.origin) return;

  // Estáticos: stale-while-revalidate. Sem sessão, sem CSRF, seguro de cachear.
  if (url.pathname.startsWith('/static/')) {
    e.respondWith(
      caches.open(CACHE_ESTATICO).then((cache) =>
        cache.match(req).then((cacheado) => {
          const rede = fetch(req)
            .then((resp) => { if (resp.ok) cache.put(req, resp.clone()); return resp; })
            .catch(() => cacheado);
          return cacheado || rede;
        })
      )
    );
    return;
  }

  // Navegação: rede primeiro; se cair, a página de offline.
  if (req.mode === 'navigate') {
    e.respondWith(fetch(req).catch(() => caches.match(PAGINA_OFFLINE)));
  }

  // Todo o resto passa direto, sem tocar.
});

self.addEventListener('push', (e) => {
  let d = {};
  // Alguns servidores de push mandam evento sem payload; uma exceção aqui
  // derruba o handler inteiro em silêncio.
  try { d = e.data ? e.data.json() : {}; } catch (_) {}

  e.waitUntil(self.registration.showNotification(d.titulo || 'PCF', {
    body: d.corpo || '',
    icon: '/static/images/icons/icon-192.png',
    badge: '/static/images/icons/badge-72.png',
    tag: d.tag || undefined,
    data: { url: d.url || '/inicio/' },
    lang: 'pt-BR',
  }));
});

self.addEventListener('notificationclick', (e) => {
  e.notification.close();
  const url = (e.notification.data && e.notification.data.url) || '/inicio/';

  // Focar uma janela já aberta em vez de abrir outra — senão o voluntário
  // acumula janelas do app.
  e.waitUntil(
    self.clients.matchAll({ type: 'window', includeUncontrolled: true }).then((lista) => {
      for (const c of lista) {
        if ('focus' in c) return c.navigate(url).then((cl) => cl && cl.focus());
      }
      return self.clients.openWindow(url);
    })
  );
});
```

- [ ] **Step 4: Montar a rota**

Modify `TESTE/urls.py` — adicionar ao import `from django.views.generic import TemplateView` e incluir em `urlpatterns`, logo depois de `path('admin/', admin.site.urls),`:

```python
    # O service worker TEM que ser servido na raiz: o escopo dele é limitado ao
    # próprio caminho, então em /static/ ele não controlaria o site.
    path('sw.js', TemplateView.as_view(
        template_name='sw.js',
        content_type='application/javascript',
    ), name='service_worker'),
```

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.ServiceWorkerTest -v2`
Expected: PASS (2 testes)

- [ ] **Step 6: Registrar o service worker no `base.html`**

Modify `templates/base.html` — antes de `</body>`, depois da linha do `pcf-fx.js`:

```html
    <script>
      /* Registra o service worker da PWA. Falha aqui não pode virar erro
         visível para o voluntário — o site funciona sem ele. */
      if ('serviceWorker' in navigator) {
        window.addEventListener('load', function () {
          navigator.serviceWorker.register('/sw.js').catch(function () {});
        });
      }
    </script>
```

---

### Task 4: App `notificacoes` e página de offline

**Files:**
- Create: `notificacoes/apps.py`, `notificacoes/urls.py`, `notificacoes/views.py`, `notificacoes/models.py`, `notificacoes/admin.py`, `notificacoes/migrations/__init__.py`
- Create: `notificacoes/templates/notificacoes/offline.html`
- Modify: `TESTE/urls.py`
- Modify: `notificacoes/tests.py`

**Interfaces:**
- Produces: namespace `notificacoes`, rota `notificacoes:offline` em `/notificacoes/offline/`

- [ ] **Step 1: Escrever o teste que falha**

Append to `notificacoes/tests.py`:

```python
class OfflineTest(TestCase):
    def test_offline_acessivel_sem_login(self):
        """Se a sessão expirou E a rede caiu, uma página de offline atrás de
        @login_required vira redirect para um login que também não carrega."""
        resposta = self.client.get("/notificacoes/offline/")
        self.assertEqual(resposta.status_code, 200)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.OfflineTest -v2`
Expected: FAIL — 404

- [ ] **Step 3: Criar os arquivos do app**

Create `notificacoes/apps.py`:

```python
from django.apps import AppConfig


class NotificacoesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "notificacoes"
    verbose_name = "Notificações"
```

Create `notificacoes/models.py` vazio por enquanto (`# Models entram na Task 8.`), `notificacoes/admin.py` vazio, e `notificacoes/migrations/__init__.py` vazio.

Create `notificacoes/views.py`:

```python
from django.views.generic import TemplateView


class OfflineView(TemplateView):
    """Sem @login_required de propósito: é a tela mostrada quando não há rede."""
    template_name = "notificacoes/offline.html"
```

Create `notificacoes/urls.py`:

```python
from django.urls import path

from . import views

app_name = "notificacoes"

urlpatterns = [
    path("offline/", views.OfflineView.as_view(), name="offline"),
]
```

- [ ] **Step 4: Criar o template de offline**

Create `notificacoes/templates/notificacoes/offline.html`. **Não estende `base.html`**: o base puxa Bootstrap e fontes de CDN, que não carregam sem rede.

```html
<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="theme-color" content="#e8560f">
  <title>PCF | Sem conexão</title>
  <style>
    body { margin:0; min-height:100vh; display:flex; flex-direction:column;
           align-items:center; justify-content:center; gap:1rem; padding:2rem;
           font-family:system-ui,-apple-system,sans-serif; color:#111110;
           text-align:center; background:#faf9f7; }
    img { width:88px; height:88px; }
    h1 { font-size:1.25rem; margin:0; }
    p  { color:#57534e; margin:0; max-width:30ch; line-height:1.5; }
    button { margin-top:.5rem; padding:.7rem 1.4rem; border:0; border-radius:.75rem;
             background:#e8560f; color:#fff; font-size:.95rem; font-weight:600;
             font-family:inherit; cursor:pointer; }
  </style>
</head>
<body>
  <img src="/static/images/icons/icon-192.png" alt="PCF">
  <h1>Sem conexão</h1>
  <p>Não foi possível carregar esta tela. Verifique sua internet e tente de novo.</p>
  <button onclick="location.reload()">Tentar de novo</button>
</body>
</html>
```

- [ ] **Step 5: Montar as rotas do app**

Modify `TESTE/urls.py` — adicionar **antes** do `path('', include('revista.urls', ...))` (que monta na raiz):

```python
    path('notificacoes/', include('notificacoes.urls', namespace='notificacoes')),
```

- [ ] **Step 6: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes -v2`
Expected: PASS (todos os testes das Tasks 2, 3 e 4)

---

### Task 5: Tela de instalação (parte de instalação)

**Files:**
- Create: `notificacoes/templates/notificacoes/instalar.html`
- Modify: `notificacoes/views.py`, `notificacoes/urls.py`, `notificacoes/tests.py`

**Interfaces:**
- Produces: rota `notificacoes:instalar` em `/notificacoes/instalar/`, exigindo login

Esta é a peça que decide se o projeto pega ou não — sem ela, ninguém no iPhone instala. A parte de push entra na Task 11.

- [ ] **Step 1: Escrever o teste que falha**

Append to `notificacoes/tests.py`:

```python
from django.contrib.auth import get_user_model
from django.urls import reverse

Voluntario = get_user_model()


class InstalarTest(TestCase):
    def setUp(self):
        self.voluntario = Voluntario.objects.create_user(
            username="ana", password="senha-de-teste-123", area="AZUL"
        )

    def test_instalar_exige_login(self):
        resposta = self.client.get(reverse("notificacoes:instalar"))
        self.assertEqual(resposta.status_code, 302)

    def test_instalar_abre_para_voluntario_logado(self):
        self.client.force_login(self.voluntario)
        resposta = self.client.get(reverse("notificacoes:instalar"))
        self.assertEqual(resposta.status_code, 200)
        self.assertTemplateUsed(resposta, "notificacoes/instalar.html")
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.InstalarTest -v2`
Expected: FAIL — `NoReverseMatch: 'instalar' is not a valid view function or pattern name`

- [ ] **Step 3: Criar a view**

Append to `notificacoes/views.py`:

```python
from django.contrib.auth.mixins import LoginRequiredMixin


class InstalarView(LoginRequiredMixin, TemplateView):
    template_name = "notificacoes/instalar.html"
```

Append to `notificacoes/urls.py` (dentro de `urlpatterns`):

```python
    path("instalar/", views.InstalarView.as_view(), name="instalar"),
```

- [ ] **Step 4: Criar o template**

Create `notificacoes/templates/notificacoes/instalar.html`:

```html
{% extends 'base.html' %}
{% block titulo %}Instalar no celular{% endblock %}

{% block content %}
<div class="container py-4" style="max-width:640px">

  <h1 style="font-size:1.5rem;font-weight:800;letter-spacing:-.02em;margin-bottom:.4rem">
    Instalar o PCF no celular
  </h1>
  <p style="color:hsl(var(--muted-foreground));margin-bottom:1.5rem">
    O PCF vira um app com ícone próprio, abre em tela cheia e pode avisar você
    por notificação. Não precisa baixar nada de loja nenhuma.
  </p>

  {# Cada bloco aparece conforme o aparelho; o JS no fim revela só um. #}

  <div data-bloco="instalado" hidden class="pcf-card" style="padding:1.25rem">
    <p style="margin:0;font-weight:600">✅ O app já está instalado neste aparelho.</p>
  </div>

  <div data-bloco="android" hidden class="pcf-card" style="padding:1.25rem">
    <p style="margin-top:0;font-weight:600">No Android</p>
    <button data-acao="instalar-android" class="pcf-btn" hidden
            style="margin-bottom:1rem">Instalar app</button>
    <ol style="margin:0;padding-left:1.2rem;line-height:1.9">
      <li>Toque no menu <b>⋮</b> do Chrome, no canto superior direito.</li>
      <li>Escolha <b>Adicionar à tela inicial</b>.</li>
      <li>Confirme em <b>Instalar</b>.</li>
    </ol>
  </div>

  <div data-bloco="ios-safari" hidden class="pcf-card" style="padding:1.25rem">
    <p style="margin-top:0;font-weight:600">No iPhone ou iPad</p>
    <ol style="margin:0;padding-left:1.2rem;line-height:1.9">
      <li>Toque em <b>Compartilhar</b> — o quadrado com a seta para cima, na barra de baixo.</li>
      <li>Role a lista e toque em <b>Adicionar à Tela de Início</b>.</li>
      <li>Toque em <b>Adicionar</b>, no canto superior direito.</li>
      <li><b>Feche o Safari e abra o PCF pelo ícone novo.</b> Este passo não é
          opcional: o iPhone só entrega notificação para app aberto pelo ícone.</li>
    </ol>
  </div>

  <div data-bloco="ios-outro" hidden class="pcf-card" style="padding:1.25rem">
    <p style="margin:0;font-weight:600">Abra esta página no Safari</p>
    <p style="margin:.5rem 0 0;color:hsl(var(--muted-foreground))">
      No iPhone, só o Safari consegue instalar o app na tela de início.
      Chrome, Firefox e Edge não têm essa opção.
    </p>
  </div>

  <div data-bloco="desktop" hidden class="pcf-card" style="padding:1.25rem">
    <p style="margin:0">
      Você está no computador. A instalação vale a pena mesmo é no celular —
      abra <b>pcf.pythonanywhere.com</b> no seu telefone para instalar lá.
    </p>
  </div>

</div>

<script>
(function () {
  const mostrar = (nome) => {
    const el = document.querySelector(`[data-bloco="${nome}"]`);
    if (el) el.hidden = false;
  };

  // window.navigator.standalone é específico do iOS e é o único sinal
  // confiável lá; display-mode cobre Android e desktop.
  const instalado = window.matchMedia('(display-mode: standalone)').matches
                 || window.navigator.standalone === true;
  const ua = navigator.userAgent;
  const iOS = /iP(hone|ad|od)/.test(ua)
           || (navigator.platform === 'MacIntel' && navigator.maxTouchPoints > 1);
  const safari = iOS && !/CriOS|FxiOS|EdgiOS|OPiOS/.test(ua);
  const android = /Android/.test(ua);

  if (instalado)      mostrar('instalado');
  else if (android)   mostrar('android');
  else if (iOS && safari) mostrar('ios-safari');
  else if (iOS)       mostrar('ios-outro');
  else                mostrar('desktop');

  // No Android o Chrome oferece instalação de 1 toque; guardamos o evento
  // porque ele só pode ser usado dentro de um gesto do usuário.
  let promptInstalacao = null;
  const botao = document.querySelector('[data-acao="instalar-android"]');

  window.addEventListener('beforeinstallprompt', (e) => {
    e.preventDefault();
    promptInstalacao = e;
    if (botao) botao.hidden = false;
  });

  if (botao) {
    botao.addEventListener('click', async () => {
      if (!promptInstalacao) return;
      promptInstalacao.prompt();
      await promptInstalacao.userChoice;
      promptInstalacao = null;
      botao.hidden = true;
    });
  }
})();
</script>
{% endblock %}
```

**Nota:** conferir se as classes `pcf-card` e `pcf-btn` existem em `static/css/pcf.css`. Se não existirem, usar as classes equivalentes do design system que estiverem lá — **não inventar classe nova** nem cair no Bootstrap.

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.InstalarTest -v2`
Expected: PASS (2 testes)

---

### Task 6: Link na sidebar

**Files:**
- Modify: `templates/sidebar.html` (menu de perfil, entre as linhas 338 e 346)

- [ ] **Step 1: Adicionar o item de menu**

Modify `templates/sidebar.html` — inserir logo depois do link `voluntario:alterar_senha` (linha ~342) e antes do `<form>` de logout:

```html
        <a class="pcf-menuitem" href="{% url 'notificacoes:instalar' %}" role="menuitem">
          <svg width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
            <rect x="5" y="2" width="14" height="20" rx="2"/><line x1="12" y1="18" x2="12.01" y2="18"/>
          </svg>
          Instalar no celular
        </a>
```

Sem banner automático em `/inicio/` nesta versão: banner intrusivo é a forma mais rápida de queimar a permissão de notificação.

- [ ] **Step 2: Conferir visualmente**

Run: `docker compose up -d`, abrir `http://localhost:8000/inicio/`, clicar no avatar
Expected: "Instalar no celular" aparece no menu, entre "Alterar senha" e "Sair", e leva à tela.

- [ ] **Step 3: Rodar a suíte inteira**

Run: `docker compose run --rm web python manage.py test`
Expected: PASS — nenhuma regressão nos apps existentes.

**🔬 ACEITE DA FASE 1 (manual, não pode ser pulado):** instalar de verdade num Android e num iPhone seguindo o passo a passo, e confirmar que abre em tela cheia, sem barra de endereço, com o ícone correto.

---

# FASE 2 — Web Push

---

### Task 7: Dependência, settings e chaves VAPID

**Files:**
- Modify: `requirements.txt`, `TESTE/settings.py`, `.env.example`
- Create: `notificacoes/management/commands/gerar_chaves_vapid.py`

**Interfaces:**
- Produces: `settings.VAPID_PUBLIC_KEY`, `settings.VAPID_PRIVATE_KEY`, `settings.VAPID_ADMIN_EMAIL`

- [ ] **Step 1: Adicionar a dependência**

Append to `requirements.txt`:

```
# Notificações push (PWA). Traz junto py-vapid, http-ece e cryptography.
# Exige Python 3.10+ — o Dockerfile usa 3.11, mas CONFIRA a versão da web app
# no PythonAnywhere antes de subir.
pywebpush==2.4.0
```

- [ ] **Step 2: Reconstruir a imagem**

Run: `docker compose build web`
Expected: instala `pywebpush`, `py-vapid`, `http-ece`, `cryptography` sem erro.

- [ ] **Step 3: Adicionar as settings**

Modify `TESTE/settings.py` — depois do bloco de e-mail:

```python
# ─── Notificações push (Web Push / VAPID) ───
# Gerar uma única vez com: python manage.py gerar_chaves_vapid
# TROCAR a chave pública invalida TODAS as inscrições existentes e obriga todo
# mundo a reativar as notificações no aparelho.
# default="" de propósito: sem isso, o projeto quebra em qualquer máquina sem
# as chaves — incluindo a suíte de testes.
VAPID_PUBLIC_KEY = config("VAPID_PUBLIC_KEY", default="")
VAPID_PRIVATE_KEY = config("VAPID_PRIVATE_KEY", default="")
VAPID_ADMIN_EMAIL = config("VAPID_ADMIN_EMAIL", default="")
```

E adicionar ao `LOGGING['loggers']`:

```python
        'notificacoes': {
            'handlers': ['console'],
            'level': 'INFO',
            'propagate': False,
        },
```

Append to `.env.example`:

```
# Notificações push. Gerar com: python manage.py gerar_chaves_vapid
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_ADMIN_EMAIL=
```

- [ ] **Step 4: Criar o comando de geração de chaves**

Create `notificacoes/management/commands/gerar_chaves_vapid.py`:

```python
"""Gera o par de chaves VAPID usado para assinar as notificações push.

RODAR UMA VEZ SÓ. Trocar a chave pública invalida todas as inscrições já
feitas e obriga cada voluntário a reativar as notificações no aparelho dele.
"""
import base64

from cryptography.hazmat.primitives.serialization import Encoding, PublicFormat
from django.core.management.base import BaseCommand
from py_vapid import Vapid02


def _b64(dados: bytes) -> str:
    """base64url sem padding — o formato que o navegador e o pywebpush esperam."""
    return base64.urlsafe_b64encode(dados).decode().rstrip("=")


class Command(BaseCommand):
    help = "Gera o par de chaves VAPID para colar no .env"

    def handle(self, *args, **options):
        vapid = Vapid02()
        vapid.generate_keys()

        privada = _b64(vapid.private_key.private_numbers().private_value.to_bytes(32, "big"))
        publica = _b64(vapid.public_key.public_bytes(
            Encoding.X962, PublicFormat.UncompressedPoint
        ))

        self.stdout.write(self.style.SUCCESS("Chaves geradas. Cole no .env:\n"))
        self.stdout.write(f"VAPID_PUBLIC_KEY={publica}")
        self.stdout.write(f"VAPID_PRIVATE_KEY={privada}")
        self.stdout.write("VAPID_ADMIN_EMAIL=<e-mail de contato do projeto>\n")
        self.stdout.write(self.style.WARNING(
            "ATENÇÃO: guarde estas chaves. Gerar de novo invalida todas as "
            "inscrições e todo mundo precisa reativar as notificações."
        ))
```

- [ ] **Step 5: Gerar e guardar as chaves**

Run: `docker compose run --rm web python manage.py gerar_chaves_vapid`
Expected: imprime as três linhas. **Colar no `.env` local e no `.env` do PythonAnywhere.**

---

### Task 8: Models `InscricaoPush` e `Aviso`

**Files:**
- Modify: `notificacoes/models.py`, `notificacoes/admin.py`, `notificacoes/tests.py`
- Create: `notificacoes/migrations/0001_initial.py` (via `makemigrations`)

**Interfaces:**
- Produces: `InscricaoPush(voluntario, endpoint, p256dh, auth, user_agent, criado_em, ultimo_ok)`, `Aviso(autor, titulo, mensagem, destino, alvo, criado_em, total_enviado)`, constante `DESTINO_CHOICES`

- [ ] **Step 1: Escrever o teste que falha**

Append to `notificacoes/tests.py`:

```python
from notificacoes.models import Aviso, InscricaoPush


class InscricaoPushModelTest(TestCase):
    def setUp(self):
        self.ana = Voluntario.objects.create_user(
            username="ana", password="senha-de-teste-123", area="AZUL"
        )

    def test_endpoint_e_unico(self):
        """O navegador reemite o mesmo endpoint ao reinscrever — a linha tem que
        ser atualizada, nunca duplicada."""
        from django.db import IntegrityError

        InscricaoPush.objects.create(
            voluntario=self.ana, endpoint="https://push.exemplo/abc",
            p256dh="chave-p256", auth="chave-auth",
        )
        with self.assertRaises(IntegrityError):
            InscricaoPush.objects.create(
                voluntario=self.ana, endpoint="https://push.exemplo/abc",
                p256dh="outra", auth="outra",
            )

    def test_voluntario_pode_ter_varios_aparelhos(self):
        for n in range(3):
            InscricaoPush.objects.create(
                voluntario=self.ana, endpoint=f"https://push.exemplo/{n}",
                p256dh="p", auth="a",
            )
        self.assertEqual(self.ana.inscricoes_push.count(), 3)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.InscricaoPushModelTest -v2`
Expected: FAIL — `ImportError: cannot import name 'InscricaoPush'`

- [ ] **Step 3: Escrever os models**

Replace `notificacoes/models.py`:

```python
from django.conf import settings
from django.db import models

# As 7 salas (VIOLETA…VERMELHO) e FAMILIA_FELIZ são valores de LISTA_AREAS, no
# mesmo campo Voluntario.area das áreas funcionais. Por isso NÃO existe um
# destino "por sala" separado: ele filtraria exatamente o mesmo campo que AREA.
DESTINO_CHOICES = [
    ("TODOS", "Todos os voluntários"),
    ("AREA", "Por área"),
]


class InscricaoPush(models.Model):
    """Um aparelho+navegador inscrito para receber push.

    Um voluntário tem várias: celular, tablet, desktop do escritório.
    """
    voluntario = models.ForeignKey(
        settings.AUTH_USER_MODEL, on_delete=models.CASCADE,
        related_name="inscricoes_push",
    )
    # Endpoints do FCM passam de 200 caracteres com folga.
    endpoint = models.URLField(max_length=500, unique=True)
    p256dh = models.CharField(max_length=255)
    auth = models.CharField(max_length=255)
    user_agent = models.CharField(max_length=255, blank=True)
    criado_em = models.DateTimeField(auto_now_add=True)
    ultimo_ok = models.DateTimeField(null=True, blank=True)

    class Meta:
        verbose_name = "inscrição push"
        verbose_name_plural = "inscrições push"
        ordering = ("-criado_em",)

    def __str__(self):
        return f"{self.voluntario} — {self.user_agent or 'aparelho desconhecido'}"


class Aviso(models.Model):
    """Registro de um aviso disparado manualmente pela gestão.

    Os outros gatilhos de push já deixam rastro nos modelos deles (Ocorrencia,
    Pedido, DisponibilidadeVoluntario), então não replicamos registro aqui.
    """
    autor = models.ForeignKey(
        settings.AUTH_USER_MODEL, on_delete=models.SET_NULL, null=True,
        related_name="avisos_enviados",
    )
    # Android trunca notificação longa e o iOS mostra ~4 linhas: os limites
    # existem para o texto caber na tela do aparelho.
    titulo = models.CharField(max_length=80)
    mensagem = models.CharField(max_length=300)
    destino = models.CharField(max_length=10, choices=DESTINO_CHOICES)
    alvo = models.CharField(max_length=30, blank=True, help_text="Valor de LISTA_AREAS")
    criado_em = models.DateTimeField(auto_now_add=True)
    total_enviado = models.PositiveIntegerField(default=0)

    class Meta:
        verbose_name = "aviso"
        verbose_name_plural = "avisos"
        ordering = ("-criado_em",)

    def __str__(self):
        return f"{self.titulo} ({self.criado_em:%d/%m/%Y})"
```

- [ ] **Step 4: Gerar e aplicar a migration**

Run: `docker compose run --rm web python manage.py makemigrations notificacoes`
Run: `docker compose run --rm web python manage.py migrate`
Expected: cria `0001_initial.py` com as duas tabelas.

- [ ] **Step 5: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.InscricaoPushModelTest -v2`
Expected: PASS (2 testes)

- [ ] **Step 6: Registrar no admin**

Replace `notificacoes/admin.py`:

```python
from django.contrib import admin

from .models import Aviso, InscricaoPush


@admin.register(InscricaoPush)
class InscricaoPushAdmin(admin.ModelAdmin):
    """Tudo readonly: ninguém edita chave de criptografia na mão — só apaga.

    Diferente do resto do projeto, NÃO usa ImportExportModelAdmin de propósito:
    exportar chave de push para planilha é vazamento de credencial.
    """
    list_display = ("voluntario", "user_agent", "criado_em", "ultimo_ok")
    search_fields = ("voluntario__username", "voluntario__first_name", "voluntario__last_name")
    list_filter = ("criado_em",)
    readonly_fields = ("voluntario", "endpoint", "p256dh", "auth",
                       "user_agent", "criado_em", "ultimo_ok")


@admin.register(Aviso)
class AvisoAdmin(admin.ModelAdmin):
    """Registro histórico — readonly."""
    list_display = ("titulo", "autor", "destino", "alvo", "total_enviado", "criado_em")
    search_fields = ("titulo", "mensagem")
    list_filter = ("destino", "criado_em")
    readonly_fields = ("autor", "titulo", "mensagem", "destino", "alvo",
                       "criado_em", "total_enviado")
```

---

### Task 9: Serviço de envio

**Files:**
- Create: `notificacoes/services.py`
- Modify: `notificacoes/tests.py`

**Interfaces:**
- Produces:
  - `enviar_push(voluntarios, titulo, corpo, url="/inicio/", tag=None) -> int`
  - `enviar_push_async(voluntarios, titulo, corpo, url="/inicio/", tag=None) -> None`

É o coração da Fase 2. Todos os quatro gatilhos consomem estas duas funções.

- [ ] **Step 1: Escrever os testes que falham**

Append to `notificacoes/tests.py`:

```python
import json
from unittest.mock import patch

from django.test import override_settings
from django.utils import timezone
from pywebpush import WebPushException


def _resposta(status):
    """Objeto mínimo no formato que o pywebpush anexa à exceção."""
    class _R:
        status_code = status
    return _R()


@override_settings(VAPID_PRIVATE_KEY="chave-falsa", VAPID_ADMIN_EMAIL="pcf@exemplo.org")
class EnviarPushTest(TestCase):
    def setUp(self):
        from notificacoes.services import enviar_push
        self.enviar_push = enviar_push
        self.ana = Voluntario.objects.create_user(
            username="ana", password="senha-de-teste-123", area="AZUL"
        )

    def _inscrever(self, sufixo, voluntario=None):
        return InscricaoPush.objects.create(
            voluntario=voluntario or self.ana,
            endpoint=f"https://push.exemplo/{sufixo}",
            p256dh="p256", auth="auth",
        )

    @patch("notificacoes.services.webpush")
    def test_envia_uma_vez_por_inscricao(self, mock_webpush):
        self._inscrever("a")
        enviados = self.enviar_push([self.ana], "Oi", "Corpo")
        self.assertEqual(enviados, 1)
        self.assertEqual(mock_webpush.call_count, 1)

    @patch("notificacoes.services.webpush")
    def test_voluntario_com_dois_aparelhos_recebe_duas_vezes(self, mock_webpush):
        self._inscrever("a")
        self._inscrever("b")
        self.assertEqual(self.enviar_push([self.ana], "Oi", "Corpo"), 2)
        self.assertEqual(mock_webpush.call_count, 2)

    @patch("notificacoes.services.webpush")
    def test_payload_carrega_titulo_corpo_e_url(self, mock_webpush):
        self._inscrever("a")
        self.enviar_push([self.ana], "Título", "Corpo", url="/voluntario/saas/")
        payload = json.loads(mock_webpush.call_args.kwargs["data"])
        self.assertEqual(payload["titulo"], "Título")
        self.assertEqual(payload["corpo"], "Corpo")
        self.assertEqual(payload["url"], "/voluntario/saas/")

    @patch("notificacoes.services.webpush")
    def test_410_apaga_a_inscricao(self, mock_webpush):
        """Inscrição morta (desinstalou, trocou de celular, limpou o navegador)."""
        self._inscrever("morta")
        erro = WebPushException("gone")
        erro.response = _resposta(410)
        mock_webpush.side_effect = erro

        self.assertEqual(self.enviar_push([self.ana], "Oi", "Corpo"), 0)
        self.assertEqual(InscricaoPush.objects.count(), 0)

    @patch("notificacoes.services.webpush")
    def test_500_mantem_a_inscricao_e_nao_propaga(self, mock_webpush):
        """Erro do servidor de push é temporário: não pode apagar nem estourar."""
        self._inscrever("viva")
        erro = WebPushException("boom")
        erro.response = _resposta(500)
        mock_webpush.side_effect = erro

        self.assertEqual(self.enviar_push([self.ana], "Oi", "Corpo"), 0)
        self.assertEqual(InscricaoPush.objects.count(), 1)

    @patch("notificacoes.services.webpush")
    def test_falha_numa_inscricao_nao_impede_as_outras(self, mock_webpush):
        """Uma inscrição podre não pode derrubar o envio das outras 59."""
        self._inscrever("ruim")
        self._inscrever("boa")
        erro = WebPushException("boom")
        erro.response = _resposta(500)
        mock_webpush.side_effect = [erro, None]

        self.assertEqual(self.enviar_push([self.ana], "Oi", "Corpo"), 1)
        self.assertEqual(mock_webpush.call_count, 2)

    @patch("notificacoes.services.webpush")
    def test_sucesso_grava_ultimo_ok(self, mock_webpush):
        inscricao = self._inscrever("a")
        self.assertIsNone(inscricao.ultimo_ok)
        self.enviar_push([self.ana], "Oi", "Corpo")
        inscricao.refresh_from_db()
        self.assertIsNotNone(inscricao.ultimo_ok)

    @patch("notificacoes.services.webpush")
    def test_claims_vapid_sao_um_dict_novo_a_cada_envio(self, mock_webpush):
        """REGRESSÃO: o pywebpush MUTA o dict de claims que recebe (grava 'exp'
        dentro dele). Reutilizar um dict de módulo faz o primeiro envio passar e
        todos os seguintes falharem com token expirado."""
        self._inscrever("a")
        self._inscrever("b")
        self.enviar_push([self.ana], "Oi", "Corpo")

        primeiro = mock_webpush.call_args_list[0].kwargs["vapid_claims"]
        segundo = mock_webpush.call_args_list[1].kwargs["vapid_claims"]
        self.assertIsNot(primeiro, segundo)

    @patch("notificacoes.services.webpush")
    def test_sem_chave_configurada_nao_envia(self, mock_webpush):
        """Mantém dev local e a suíte funcionando sem chaves VAPID."""
        self._inscrever("a")
        with override_settings(VAPID_PRIVATE_KEY=""):
            self.assertEqual(self.enviar_push([self.ana], "Oi", "Corpo"), 0)
        mock_webpush.assert_not_called()

    @patch("notificacoes.services.webpush")
    def test_nao_envia_para_quem_nao_esta_na_lista(self, mock_webpush):
        bruno = Voluntario.objects.create_user(
            username="bruno", password="senha-de-teste-123", area="VERDE"
        )
        self._inscrever("ana")
        self._inscrever("bruno", voluntario=bruno)

        self.assertEqual(self.enviar_push([self.ana], "Oi", "Corpo"), 1)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.EnviarPushTest -v2`
Expected: FAIL — `ModuleNotFoundError: No module named 'notificacoes.services'`

- [ ] **Step 3: Escrever o serviço**

Create `notificacoes/services.py`:

```python
"""Envio de notificações push.

Interface única consumida pelos quatro gatilhos do sistema. O push SOMA ao
e-mail que já existe — nunca substitui.
"""
import json
import logging
import threading

from django.conf import settings
from django.utils import timezone
from pywebpush import WebPushException, webpush

from .models import InscricaoPush

logger = logging.getLogger("notificacoes")

# Status que significam "esta inscrição morreu": o aparelho desinstalou o app,
# trocou de dono ou limpou o navegador.
STATUS_INSCRICAO_MORTA = (404, 410)

TTL_SEGUNDOS = 86400  # o servidor de push segura por até 1 dia se o aparelho estiver offline


def enviar_push(voluntarios, titulo, corpo, url="/inicio/", tag=None) -> int:
    """Envia um push para todas as inscrições dos voluntários dados.

    Retorna quantos aparelhos receberam. Nunca levanta exceção: falha de push
    não pode derrubar a request nem o comando que chamou.
    """
    if not settings.VAPID_PRIVATE_KEY:
        logger.warning("VAPID_PRIVATE_KEY não configurada — push não enviado.")
        return 0

    inscricoes = InscricaoPush.objects.filter(voluntario__in=voluntarios)
    payload = json.dumps({"titulo": titulo, "corpo": corpo, "url": url, "tag": tag})

    enviados = 0
    for inscricao in inscricoes:
        try:
            webpush(
                subscription_info={
                    "endpoint": inscricao.endpoint,
                    "keys": {"p256dh": inscricao.p256dh, "auth": inscricao.auth},
                },
                data=payload,
                vapid_private_key=settings.VAPID_PRIVATE_KEY,
                # Dict NOVO a cada chamada: o pywebpush grava 'exp' dentro dele.
                # Reutilizar um dict de módulo faz o segundo envio falhar com
                # token expirado.
                vapid_claims={"sub": f"mailto:{settings.VAPID_ADMIN_EMAIL}"},
                ttl=TTL_SEGUNDOS,
            )
        except WebPushException as e:
            status = getattr(getattr(e, "response", None), "status_code", None)
            if status in STATUS_INSCRICAO_MORTA:
                logger.info("Inscrição morta (%s) removida: %s", status, inscricao.endpoint[:60])
                inscricao.delete()
            else:
                logger.warning("Falha ao enviar push (%s): %s", status, inscricao.endpoint[:60])
        except Exception:
            # Qualquer outro erro segue o mesmo princípio: registra e continua.
            logger.exception("Erro inesperado ao enviar push para %s", inscricao.endpoint[:60])
        else:
            inscricao.ultimo_ok = timezone.now()
            inscricao.save(update_fields=["ultimo_ok"])
            enviados += 1

    return enviados


def enviar_push_async(voluntarios, titulo, corpo, url="/inicio/", tag=None) -> None:
    """Igual a enviar_push, em thread daemon.

    Usar no caminho de request, para não segurar a resposta HTTP — mesmo padrão
    do envio de e-mail de ocorrências em voluntario/views.py.

    NÃO usar em management command: a thread daemon morre quando o processo
    encerra e a notificação some.
    """
    threading.Thread(
        target=enviar_push,
        args=(list(voluntarios), titulo, corpo, url, tag),
        daemon=True,
    ).start()
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.EnviarPushTest -v2`
Expected: PASS (10 testes)

---

### Task 10: Views de inscrição

**Files:**
- Modify: `notificacoes/views.py`, `notificacoes/urls.py`, `notificacoes/tests.py`

**Interfaces:**
- Produces: `notificacoes:inscrever`, `notificacoes:desinscrever`, `notificacoes:testar`

- [ ] **Step 1: Escrever os testes que falham**

Append to `notificacoes/tests.py`:

```python
class InscreverTest(TestCase):
    def setUp(self):
        self.ana = Voluntario.objects.create_user(
            username="ana", password="senha-de-teste-123", area="AZUL"
        )
        self.url = reverse("notificacoes:inscrever")
        self.corpo = {
            "endpoint": "https://push.exemplo/abc",
            "keys": {"p256dh": "chave-p256", "auth": "chave-auth"},
        }

    def _postar(self, corpo):
        return self.client.post(
            self.url, data=json.dumps(corpo), content_type="application/json"
        )

    def test_exige_login(self):
        resposta = self._postar(self.corpo)
        self.assertEqual(resposta.status_code, 302)
        self.assertEqual(InscricaoPush.objects.count(), 0)

    def test_cria_a_inscricao(self):
        self.client.force_login(self.ana)
        resposta = self._postar(self.corpo)
        self.assertEqual(resposta.status_code, 200)
        inscricao = InscricaoPush.objects.get()
        self.assertEqual(inscricao.voluntario, self.ana)
        self.assertEqual(inscricao.p256dh, "chave-p256")

    def test_mesmo_endpoint_atualiza_em_vez_de_duplicar(self):
        self.client.force_login(self.ana)
        self._postar(self.corpo)
        self.corpo["keys"]["p256dh"] = "chave-nova"
        self._postar(self.corpo)

        self.assertEqual(InscricaoPush.objects.count(), 1)
        self.assertEqual(InscricaoPush.objects.get().p256dh, "chave-nova")

    def test_sem_p256dh_da_400(self):
        self.client.force_login(self.ana)
        resposta = self._postar({"endpoint": "https://push.exemplo/x", "keys": {"auth": "a"}})
        self.assertEqual(resposta.status_code, 400)
        self.assertEqual(InscricaoPush.objects.count(), 0)

    def test_grava_o_user_agent(self):
        self.client.force_login(self.ana)
        self.client.post(
            self.url, data=json.dumps(self.corpo),
            content_type="application/json", HTTP_USER_AGENT="iPhone Safari",
        )
        self.assertEqual(InscricaoPush.objects.get().user_agent, "iPhone Safari")


class DesinscreverTest(TestCase):
    def setUp(self):
        self.ana = Voluntario.objects.create_user(
            username="ana", password="senha-de-teste-123", area="AZUL"
        )
        self.bruno = Voluntario.objects.create_user(
            username="bruno", password="senha-de-teste-123", area="VERDE"
        )
        self.inscricao_do_bruno = InscricaoPush.objects.create(
            voluntario=self.bruno, endpoint="https://push.exemplo/bruno",
            p256dh="p", auth="a",
        )

    def test_nao_apaga_inscricao_de_outro_voluntario(self):
        """Sem filtrar por voluntário, um POST forjado apaga inscrição alheia."""
        self.client.force_login(self.ana)
        self.client.post(
            reverse("notificacoes:desinscrever"),
            data=json.dumps({"endpoint": "https://push.exemplo/bruno"}),
            content_type="application/json",
        )
        self.assertTrue(InscricaoPush.objects.filter(pk=self.inscricao_do_bruno.pk).exists())

    def test_apaga_a_propria_inscricao(self):
        InscricaoPush.objects.create(
            voluntario=self.ana, endpoint="https://push.exemplo/ana", p256dh="p", auth="a"
        )
        self.client.force_login(self.ana)
        self.client.post(
            reverse("notificacoes:desinscrever"),
            data=json.dumps({"endpoint": "https://push.exemplo/ana"}),
            content_type="application/json",
        )
        self.assertFalse(InscricaoPush.objects.filter(voluntario=self.ana).exists())
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.InscreverTest notificacoes.tests.DesinscreverTest -v2`
Expected: FAIL — `NoReverseMatch: 'inscrever'`

- [ ] **Step 3: Escrever as views**

Append to `notificacoes/views.py`:

```python
import json

from django.contrib.auth.decorators import login_required
from django.http import JsonResponse
from django.views.decorators.http import require_POST

from .models import InscricaoPush
from .services import enviar_push


def _corpo_json(request):
    try:
        return json.loads(request.body or "{}")
    except json.JSONDecodeError:
        return {}


@login_required
@require_POST
def inscrever(request):
    """Grava a inscrição de push do aparelho.

    Chaveado por endpoint, não por voluntário: o navegador reemite o mesmo
    endpoint ao reinscrever, e num aparelho compartilhado a inscrição precisa
    passar para quem logou por último.
    """
    dados = _corpo_json(request)
    endpoint = dados.get("endpoint")
    chaves = dados.get("keys") or {}
    p256dh, auth = chaves.get("p256dh"), chaves.get("auth")

    if not (endpoint and p256dh and auth):
        return JsonResponse({"ok": False, "erro": "inscrição incompleta"}, status=400)

    InscricaoPush.objects.update_or_create(
        endpoint=endpoint,
        defaults={
            "voluntario": request.user,
            "p256dh": p256dh,
            "auth": auth,
            "user_agent": request.META.get("HTTP_USER_AGENT", "")[:255],
        },
    )
    return JsonResponse({"ok": True})


@login_required
@require_POST
def desinscrever(request):
    endpoint = _corpo_json(request).get("endpoint")
    if endpoint:
        # Filtrar pelo voluntário também: sem isso, um POST forjado apaga
        # inscrição de outra pessoa.
        InscricaoPush.objects.filter(voluntario=request.user, endpoint=endpoint).delete()
    return JsonResponse({"ok": True})


@login_required
@require_POST
def testar(request):
    """Dispara um push de teste para o próprio usuário.

    Síncrono de propósito: o voluntário está olhando para a tela esperando o
    resultado. É a ferramenta de diagnóstico do onboarding.
    """
    enviados = enviar_push(
        [request.user],
        "Funcionou! 🎉",
        "As notificações do PCF estão ativas neste aparelho.",
        url="/notificacoes/instalar/",
        tag="teste",
    )
    return JsonResponse({"ok": enviados > 0, "enviados": enviados})
```

Append to `notificacoes/urls.py` (dentro de `urlpatterns`):

```python
    path("inscrever/", views.inscrever, name="inscrever"),
    path("desinscrever/", views.desinscrever, name="desinscrever"),
    path("testar/", views.testar, name="testar"),
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes -v2`
Expected: PASS (tudo até aqui)

---

### Task 11: Ativação de push na tela de instalação

**Files:**
- Modify: `notificacoes/views.py` (contexto da `InstalarView`), `notificacoes/templates/notificacoes/instalar.html`

**Interfaces:**
- Consumes: `notificacoes:inscrever`, `notificacoes:testar`, `settings.VAPID_PUBLIC_KEY`

- [ ] **Step 1: Passar a chave pública no contexto**

Modify `notificacoes/views.py` — substituir a `InstalarView`:

```python
class InstalarView(LoginRequiredMixin, TemplateView):
    template_name = "notificacoes/instalar.html"

    def get_context_data(self, **kwargs):
        contexto = super().get_context_data(**kwargs)
        contexto["vapid_public_key"] = settings.VAPID_PUBLIC_KEY
        contexto["push_disponivel"] = bool(settings.VAPID_PUBLIC_KEY)
        contexto["inscricoes"] = self.request.user.inscricoes_push.all()
        return contexto
```

(adicionar `from django.conf import settings` no topo)

- [ ] **Step 2: Adicionar o bloco de notificações no template**

Modify `notificacoes/templates/notificacoes/instalar.html` — inserir depois do bloco `data-bloco="instalado"` e antes de `data-bloco="android"`:

```html
  {% if push_disponivel %}
  <div data-bloco="push" hidden class="pcf-card" style="padding:1.25rem;margin-top:1rem"
       data-chave="{{ vapid_public_key }}" data-csrf="{{ csrf_token }}">

    <p style="margin-top:0;font-weight:600">Notificações</p>

    <div data-push="pedir">
      <p style="color:hsl(var(--muted-foreground));margin:.4rem 0 1rem">
        Receba aviso de enquete do sábado, ocorrências e recados da gestão.
      </p>
      <button data-acao="ativar-push" class="pcf-btn">Ativar notificações</button>
    </div>

    <div data-push="ativo" hidden>
      <p style="margin:.4rem 0 1rem">✅ Notificações ativas neste aparelho.</p>
      <button data-acao="testar-push" class="pcf-btn">Enviar notificação de teste</button>
      <p data-push="resultado-teste" style="margin:.75rem 0 0;font-size:.9rem"></p>
    </div>

    <div data-push="negado" hidden>
      <p style="margin:.4rem 0 0">
        As notificações estão <b>bloqueadas</b> neste aparelho. Para reverter, abra
        as configurações do site no navegador (o cadeado ou o ⓘ na barra de
        endereço), encontre <b>Notificações</b> e mude para <b>Permitir</b>.
      </p>
    </div>
  </div>
  {% endif %}
```

- [ ] **Step 3: Adicionar o JavaScript de inscrição**

Modify o `<script>` do template — acrescentar antes do fechamento do IIFE:

```javascript
  // ── Notificações push ──
  const blocoPush = document.querySelector('[data-bloco="push"]');

  // No Android o push funciona sem instalar, direto na aba do Chrome.
  // No iOS NÃO: a API de notificação nem existe fora da PWA instalada — então
  // num iPhone que ainda não instalou, o botão não pode aparecer, senão
  // mostraríamos um botão que estoura erro.
  const podePedirPush = blocoPush
    && 'serviceWorker' in navigator
    && 'PushManager' in window
    && (!iOS || instalado);

  const mostrarEstadoPush = (estado) => {
    ['pedir', 'ativo', 'negado'].forEach((nome) => {
      const el = blocoPush.querySelector(`[data-push="${nome}"]`);
      if (el) el.hidden = (nome !== estado);
    });
  };

  const base64UrlParaUint8Array = (base64) => {
    const preenchimento = '='.repeat((4 - base64.length % 4) % 4);
    const normalizado = (base64 + preenchimento).replace(/-/g, '+').replace(/_/g, '/');
    const bruto = atob(normalizado);
    return Uint8Array.from([...bruto].map((c) => c.charCodeAt(0)));
  };

  const enviarInscricao = (inscricao) =>
    fetch('{% url "notificacoes:inscrever" %}', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'X-CSRFToken': blocoPush.dataset.csrf,
      },
      body: JSON.stringify(inscricao.toJSON()),
    });

  if (podePedirPush) {
    blocoPush.hidden = false;

    navigator.serviceWorker.ready.then(async (registro) => {
      const jaInscrito = await registro.pushManager.getSubscription();
      if (Notification.permission === 'denied') mostrarEstadoPush('negado');
      else if (jaInscrito) {
        await enviarInscricao(jaInscrito);   // idempotente pelo update_or_create
        mostrarEstadoPush('ativo');
      } else mostrarEstadoPush('pedir');
    });

    blocoPush.querySelector('[data-acao="ativar-push"]')
      .addEventListener('click', async () => {
        // requestPermission SÓ dentro do gesto do usuário: o navegador pune
        // pedido no carregamento, e quem nega uma vez nega para sempre.
        const permissao = await Notification.requestPermission();
        if (permissao !== 'granted') { mostrarEstadoPush('negado'); return; }

        const registro = await navigator.serviceWorker.ready;
        const inscricao = await registro.pushManager.subscribe({
          userVisibleOnly: true,   // obrigatório: sem isso o navegador recusa
          applicationServerKey: base64UrlParaUint8Array(blocoPush.dataset.chave),
        });
        await enviarInscricao(inscricao);
        mostrarEstadoPush('ativo');
      });

    blocoPush.querySelector('[data-acao="testar-push"]')
      .addEventListener('click', async () => {
        const saida = blocoPush.querySelector('[data-push="resultado-teste"]');
        saida.textContent = 'Enviando…';
        const r = await fetch('{% url "notificacoes:testar" %}', {
          method: 'POST',
          headers: { 'X-CSRFToken': blocoPush.dataset.csrf },
        });
        const dados = await r.json();
        saida.textContent = dados.enviados
          ? `Enviado para ${dados.enviados} aparelho(s). Deve chegar em segundos.`
          : 'Não foi possível enviar. Tente ativar as notificações de novo.';
      });
  }
```

- [ ] **Step 4: Rodar a suíte**

Run: `docker compose run --rm web python manage.py test notificacoes -v2`
Expected: PASS

**🔬 ACEITE DA FASE 2 (manual, não pode ser pulado):** o botão "Enviar notificação de teste" entrega notificação num Android e num iPhone reais, **com o app fechado**. Testar num iPhone só depois de adicionar à Tela de Início e reabrir pelo ícone.

---

# FASE 3 — Os quatro gatilhos

---

### Task 12: Aviso geral da gestão

**Files:**
- Create: `notificacoes/forms.py`, `notificacoes/templates/notificacoes/avisos.html`
- Modify: `notificacoes/views.py`, `notificacoes/urls.py`, `notificacoes/tests.py`

**Interfaces:**
- Produces: rota `notificacoes:avisos`, restrita a `TRIADE`/`GESTAO_DE_TALENTOS`/superuser

- [ ] **Step 1: Escrever os testes que falham**

Append to `notificacoes/tests.py`:

```python
@override_settings(VAPID_PRIVATE_KEY="chave-falsa", VAPID_ADMIN_EMAIL="pcf@exemplo.org")
class AvisosTest(TestCase):
    def setUp(self):
        self.url = reverse("notificacoes:avisos")
        self.gestor = Voluntario.objects.create_user(
            username="gestor", password="senha-de-teste-123", area="TRIADE"
        )
        self.comum = Voluntario.objects.create_user(
            username="comum", password="senha-de-teste-123", area="AZUL"
        )
        self.desligado = Voluntario.objects.create_user(
            username="desligado", password="senha-de-teste-123", area="AZUL",
        )
        self.desligado.data_saida = timezone.now().date()
        self.desligado.save()

    def test_voluntario_comum_recebe_403(self):
        self.client.force_login(self.comum)
        self.assertEqual(self.client.get(self.url).status_code, 403)

    def test_triade_abre(self):
        self.client.force_login(self.gestor)
        self.assertEqual(self.client.get(self.url).status_code, 200)

    @patch("notificacoes.services.webpush")
    def test_post_cria_aviso_e_ignora_desligados(self, mock_webpush):
        """Voluntário que saiu do projeto não pode receber recado da gestão."""
        for v in (self.comum, self.desligado):
            InscricaoPush.objects.create(
                voluntario=v, endpoint=f"https://push.exemplo/{v.username}",
                p256dh="p", auth="a",
            )
        self.client.force_login(self.gestor)
        self.client.post(self.url, {
            "titulo": "Reunião", "mensagem": "Sábado às 8h", "destino": "TODOS", "alvo": "",
        })

        self.assertEqual(Aviso.objects.count(), 1)
        enviados = [c.kwargs["subscription_info"]["endpoint"] for c in mock_webpush.call_args_list]
        self.assertNotIn("https://push.exemplo/desligado", enviados)

    @patch("notificacoes.services.webpush")
    def test_destino_area_envia_so_para_a_area(self, mock_webpush):
        verde = Voluntario.objects.create_user(
            username="verde", password="senha-de-teste-123", area="VERDE"
        )
        for v in (self.comum, verde):
            InscricaoPush.objects.create(
                voluntario=v, endpoint=f"https://push.exemplo/{v.username}",
                p256dh="p", auth="a",
            )
        self.client.force_login(self.gestor)
        self.client.post(self.url, {
            "titulo": "Sala Verde", "mensagem": "Recado", "destino": "AREA", "alvo": "VERDE",
        })

        enviados = [c.kwargs["subscription_info"]["endpoint"] for c in mock_webpush.call_args_list]
        self.assertEqual(enviados, ["https://push.exemplo/verde"])
```

**Nota:** o teste usa `enviar_push` **síncrono** dentro da view? Não — a view usa `enviar_push_async`, e thread daemon torna o teste instável. **Por isso a view chama `enviar_push` diretamente** (síncrono): o público é no máximo algumas dezenas de voluntários e o gestor está olhando para a tela esperando a contagem de quantos receberam. Está registrado aqui para não ser "corrigido" depois.

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.AvisosTest -v2`
Expected: FAIL — `NoReverseMatch: 'avisos'`

- [ ] **Step 3: Criar o form**

Create `notificacoes/forms.py`:

```python
from django import forms
from voluntario.models import LISTA_AREAS

from .models import Aviso


class AvisoForm(forms.ModelForm):
    # Importa LISTA_AREAS de voluntario.models: a tupla já foi duplicada demais
    # neste projeto. As salas estão lá dentro — não existe lista separada.
    alvo = forms.ChoiceField(
        choices=[("", "—")] + list(LISTA_AREAS), required=False, label="Área ou sala"
    )

    class Meta:
        model = Aviso
        fields = ("titulo", "mensagem", "destino", "alvo")

    def clean(self):
        dados = super().clean()
        if dados.get("destino") == "AREA" and not dados.get("alvo"):
            self.add_error("alvo", "Escolha a área ou sala que vai receber o aviso.")
        if dados.get("destino") == "TODOS":
            dados["alvo"] = ""
        return dados
```

- [ ] **Step 4: Escrever a view**

Append to `notificacoes/views.py`:

```python
from django.contrib import messages
from django.core.exceptions import PermissionDenied
from django.shortcuts import redirect, render
from voluntario.models import Voluntario

from .forms import AvisoForm
from .models import Aviso

# Mesmo padrão de sabado/views.py:236.
AREAS_QUE_PODEM_AVISAR = {"TRIADE", "GESTAO_DE_TALENTOS"}


def _publico_do_aviso(aviso):
    """Voluntários que vão receber. Sempre só os ativos."""
    ativos = Voluntario.objects.ativos()
    if aviso.destino == "AREA":
        return ativos.filter(area=aviso.alvo)
    return ativos


@login_required
def avisos(request):
    if request.user.area not in AREAS_QUE_PODEM_AVISAR and not request.user.is_superuser:
        raise PermissionDenied("Só a Tríade e a Gestão de Talentos enviam avisos.")

    if request.method == "POST":
        form = AvisoForm(request.POST)
        if form.is_valid():
            aviso = form.save(commit=False)
            aviso.autor = request.user
            aviso.save()

            # Síncrono de propósito: o gestor espera a contagem na tela, e o
            # público é de algumas dezenas de pessoas.
            aviso.total_enviado = enviar_push(
                _publico_do_aviso(aviso), aviso.titulo, aviso.mensagem, url="/inicio/"
            )
            aviso.save(update_fields=["total_enviado"])

            messages.success(
                request, f"Aviso enviado para {aviso.total_enviado} aparelho(s)."
            )
            return redirect("notificacoes:avisos")
    else:
        form = AvisoForm()

    return render(request, "notificacoes/avisos.html", {
        "form": form,
        "avisos": Aviso.objects.select_related("autor")[:20],
    })
```

Append to `notificacoes/urls.py`:

```python
    path("avisos/", views.avisos, name="avisos"),
```

- [ ] **Step 5: Criar o template**

Create `notificacoes/templates/notificacoes/avisos.html`:

```html
{% extends 'base.html' %}
{% block titulo %}Avisos{% endblock %}

{% block content %}
<div class="container py-4" style="max-width:640px">
  <h1 style="font-size:1.5rem;font-weight:800;letter-spacing:-.02em">Enviar aviso</h1>
  <p style="color:hsl(var(--muted-foreground))">
    Chega como notificação no celular de quem instalou o app.
  </p>

  <form method="post" class="pcf-card" style="padding:1.25rem;margin-bottom:2rem">
    {% csrf_token %}
    {{ form.as_p }}
    <button type="submit" class="pcf-btn">Enviar</button>
  </form>

  <h2 style="font-size:1.1rem;font-weight:700">Últimos avisos</h2>
  {% for aviso in avisos %}
    <div class="pcf-card" style="padding:.9rem 1.1rem;margin-bottom:.6rem">
      <b>{{ aviso.titulo }}</b>
      <p style="margin:.25rem 0;color:hsl(var(--muted-foreground))">{{ aviso.mensagem }}</p>
      <small style="color:hsl(var(--muted-foreground))">
        {{ aviso.criado_em|date:"d/m/Y H:i" }} · {{ aviso.autor|default:"—" }} ·
        {{ aviso.total_enviado }} aparelho(s)
      </small>
    </div>
  {% empty %}
    <p style="color:hsl(var(--muted-foreground))">Nenhum aviso enviado ainda.</p>
  {% endfor %}
</div>
{% endblock %}
```

- [ ] **Step 6: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes.tests.AvisosTest -v2`
Expected: PASS (4 testes)

- [ ] **Step 7: Adicionar o link na sidebar (restrito)**

Modify `templates/sidebar.html` — no bloco de itens restritos à Tríade/GT, seguindo o padrão que já existe lá para entradas restritas:

```html
{% if user.area == 'TRIADE' or user.area == 'GESTAO_DE_TALENTOS' or user.is_superuser %}
  <a class="pcf-navitem" href="{% url 'notificacoes:avisos' %}"><span>Avisos</span></a>
{% endif %}
```

---

### Task 13: Gatilho 1 — enquete de disponibilidade

**Files:**
- Modify: `sabado/management/commands/lembrete_disponibilidade.py`
- Create: `notificacoes/tests_gatilhos.py`

- [ ] **Step 1: Escrever o teste que falha**

Create `notificacoes/tests_gatilhos.py`:

```python
"""Testes dos gatilhos de push espalhados por outros apps.

Separado de tests.py porque exercita sabado/, voluntario/ e supply/ — o
`manage.py test notificacoes` pega os dois arquivos.
"""
from datetime import timedelta
from unittest.mock import patch

from django.contrib.auth import get_user_model
from django.core.management import call_command
from django.test import TestCase, override_settings
from django.utils import timezone

from notificacoes.models import InscricaoPush
from sabado.models import Sabado

Voluntario = get_user_model()


@override_settings(VAPID_PRIVATE_KEY="chave-falsa", VAPID_ADMIN_EMAIL="pcf@exemplo.org")
class LembreteDisponibilidadeTest(TestCase):
    def setUp(self):
        self.ana = Voluntario.objects.create_user(
            username="ana", password="senha-de-teste-123",
            area="AZUL", email="ana@exemplo.org",
        )
        InscricaoPush.objects.create(
            voluntario=self.ana, endpoint="https://push.exemplo/ana", p256dh="p", auth="a"
        )
        # A enquete fecha 3 dias antes; o lembrete sai quando falta 1 dia para
        # fechar, ou seja, 4 dias antes do sábado.
        self.sabado = Sabado.objects.create(data=timezone.now().date() + timedelta(days=4))

    @patch("notificacoes.services.webpush")
    @patch("sabado.management.commands.lembrete_disponibilidade.send_mail")
    def test_manda_push_alem_do_email(self, mock_email, mock_webpush):
        """O push SOMA ao e-mail: quem não instalou continua recebendo tudo."""
        call_command("lembrete_disponibilidade")
        mock_email.assert_called_once()
        mock_webpush.assert_called_once()

    @patch("notificacoes.services.webpush")
    @patch("sabado.management.commands.lembrete_disponibilidade.send_mail")
    def test_push_sai_mesmo_sem_email_cadastrado(self, mock_email, mock_webpush):
        """Quem não tem e-mail hoje não recebe nada; com push, passa a receber."""
        self.ana.email = ""
        self.ana.save()
        call_command("lembrete_disponibilidade")
        mock_email.assert_not_called()
        mock_webpush.assert_called_once()
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests_gatilhos.LembreteDisponibilidadeTest -v2`
Expected: FAIL — `mock_webpush.assert_called_once()` com 0 chamadas

- [ ] **Step 3: Ligar o push no comando**

Modify `sabado/management/commands/lembrete_disponibilidade.py`:

Adicionar ao topo:

```python
from notificacoes.services import enviar_push
```

E dentro do `if data_fechamento - hoje == timedelta(days=1) and sabado.enquete_aberta:`, **depois** do `for voluntario in voluntarios:` (fora do loop, no mesmo nível):

```python
                # Push para todo mundo que não respondeu — inclusive quem não
                # tem e-mail cadastrado. Comando agendado usa enviar_push
                # SÍNCRONO: thread daemon morreria junto com o processo.
                data_fmt = sabado.data.strftime('%d/%m/%Y')
                enviados = enviar_push(
                    voluntarios,
                    "Responda sua disponibilidade",
                    f"A enquete do sábado {data_fmt} fecha amanhã.",
                    url="/sabado/",
                    tag=f"enquete-{sabado.pk}",
                )
                self.stdout.write(self.style.SUCCESS(f"{enviados} push enviado(s)"))
```

**Atenção:** `voluntarios` é um queryset já avaliado pelo `for` anterior; passar direto funciona. Conferir o `url="/sabado/"` contra `sabado/urls.py` e ajustar para a rota real da enquete.

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `docker compose run --rm web python manage.py test notificacoes.tests_gatilhos.LembreteDisponibilidadeTest -v2`
Expected: PASS (2 testes)

---

### Task 14: Gatilho 2 — ocorrências e alertas

**Files:**
- Modify: `voluntario/views.py` (dois pontos: ~linha 561 e ~linha 817)
- Modify: `notificacoes/tests_gatilhos.py`

Os dois pontos já disparam `threading.Thread(target=_enviar_email_ocorrencia, ...)`. O push entra ao lado, no mesmo lugar.

- [ ] **Step 1: Escrever o teste que falha**

Append to `notificacoes/tests_gatilhos.py`:

```python
@override_settings(VAPID_PRIVATE_KEY="chave-falsa", VAPID_ADMIN_EMAIL="pcf@exemplo.org")
class OcorrenciaPushTest(TestCase):
    @patch("notificacoes.services.webpush")
    def test_push_de_ocorrencia_tem_titulo_generico(self, mock_webpush):
        """Notificação disciplinar aparece na tela de bloqueio, muitas vezes com
        outra pessoa olhando o celular. O título não pode entregar o assunto."""
        import json

        from notificacoes.services import enviar_push

        ana = Voluntario.objects.create_user(
            username="ana", password="senha-de-teste-123", area="AZUL"
        )
        InscricaoPush.objects.create(
            voluntario=ana, endpoint="https://push.exemplo/ana", p256dh="p", auth="a"
        )
        enviar_push([ana], "Você recebeu um comunicado",
                    "Abra o app para ver os detalhes.", url="/voluntario/saas/")

        payload = json.loads(mock_webpush.call_args.kwargs["data"])
        self.assertEqual(payload["titulo"], "Você recebeu um comunicado")
        for palavra in ("advertência", "suspensão", "alerta", "falta"):
            self.assertNotIn(palavra, payload["titulo"].lower())
```

- [ ] **Step 2: Rodar e confirmar que passa ou falha**

Run: `docker compose run --rm web python manage.py test notificacoes.tests_gatilhos.OcorrenciaPushTest -v2`
Expected: PASS — este teste trava a regra do título; o serviço já existe.

- [ ] **Step 3: Ligar o push nos dois pontos**

Modify `voluntario/views.py` — adicionar ao topo `from notificacoes.services import enviar_push_async`.

**Ponto 1** (~linha 561), logo depois do `threading.Thread(target=_enviar_email_ocorrencia, args=(advertido, notificacoes), daemon=True).start()`:

```python
    # Push além do e-mail. Título genérico de propósito: a notificação aparece
    # na tela de bloqueio, às vezes com outra pessoa olhando o celular.
    enviar_push_async(
        [advertido],
        "Você recebeu um comunicado",
        "Abra o app para ver os detalhes.",
        url="/voluntario/saas/",
        tag=f"ocorrencia-{advertido.pk}",
    )
```

**Ponto 2** (~linha 817, dentro de `verificar_faltas_e_gerar_alertas`), logo depois do `threading.Thread(...)` equivalente:

```python
    enviar_push_async(
        [voluntario],
        "Você recebeu um comunicado",
        "Abra o app para ver os detalhes.",
        url="/voluntario/saas/",
        tag=f"ocorrencia-{voluntario.pk}",
    )
```

Conferir `/voluntario/saas/` contra `voluntario/urls.py` e ajustar se a rota tiver outro caminho.

- [ ] **Step 4: Rodar a suíte de voluntário e a de notificações**

Run: `docker compose run --rm web python manage.py test voluntario notificacoes -v2`
Expected: PASS — nenhuma regressão no fluxo de ocorrências.

---

### Task 15: Gatilho 3 — supply e reembolso

**Files:**
- Modify: `forms_pcf/views.py` (`_enviar_email`, ~linha 71-90), `supply/views.py` (`adicionar_pedidos`, ~linha 499)
- Modify: `notificacoes/tests_gatilhos.py`

- [ ] **Step 1: Escrever o teste que falha**

Append to `notificacoes/tests_gatilhos.py`:

```python
@override_settings(VAPID_PRIVATE_KEY="chave-falsa", VAPID_ADMIN_EMAIL="pcf@exemplo.org")
class PublicoSupplyTest(TestCase):
    """O público de pedidos é quem cuida de material e de dinheiro."""

    @patch("notificacoes.services.webpush")
    def test_push_vai_para_supply_e_admfin_ativos(self, mock_webpush):
        from notificacoes.services import enviar_push
        from voluntario.models import Voluntario as V

        supply = Voluntario.objects.create_user(
            username="s", password="senha-de-teste-123", area="SUPPLY"
        )
        financeiro = Voluntario.objects.create_user(
            username="f", password="senha-de-teste-123", area="ADM/FIN"
        )
        outro = Voluntario.objects.create_user(
            username="o", password="senha-de-teste-123", area="AZUL"
        )
        for v in (supply, financeiro, outro):
            InscricaoPush.objects.create(
                voluntario=v, endpoint=f"https://push.exemplo/{v.username}",
                p256dh="p", auth="a",
            )

        publico = V.objects.ativos().filter(area__in=["SUPPLY", "ADM/FIN"])
        enviar_push(publico, "Novo pedido de material", "Confira no app.")

        enviados = {c.kwargs["subscription_info"]["endpoint"] for c in mock_webpush.call_args_list}
        self.assertEqual(enviados, {"https://push.exemplo/s", "https://push.exemplo/f"})
```

- [ ] **Step 2: Rodar e confirmar**

Run: `docker compose run --rm web python manage.py test notificacoes.tests_gatilhos.PublicoSupplyTest -v2`
Expected: PASS — trava a definição do público.

- [ ] **Step 3: Ligar o push no reembolso**

Modify `forms_pcf/views.py` — no fim do método `_enviar_email`, **depois** do `send_mail(...)`:

```python
        from notificacoes.services import enviar_push_async
        from voluntario.models import Voluntario

        enviar_push_async(
            Voluntario.objects.ativos().filter(area__in=["SUPPLY", "ADM/FIN"]),
            "Novo pedido de reembolso",
            f"R$ {pedido.valor} — {nome}",
            url="/forms/reembolso/inbox/",
            tag="reembolso",
        )
```

Conferir a URL do inbox contra `forms_pcf/urls.py`.

- [ ] **Step 4: Ligar o push no pedido de material**

Modify `supply/views.py` — em `adicionar_pedidos`, depois de salvar os pedidos com sucesso (localizar o ponto onde `pedidos_para_salvar` é persistido e a `messages.success` é emitida):

```python
            from notificacoes.services import enviar_push_async
            from voluntario.models import Voluntario

            enviar_push_async(
                Voluntario.objects.ativos().filter(area="SUPPLY"),
                "Novo pedido de material",
                f"{len(pedidos_para_salvar)} item(ns) pedido(s) por "
                f"{request.user.get_full_name() or request.user.username}.",
                url="/supply/",
                tag="pedido-material",
            )
```

- [ ] **Step 5: Rodar a suíte inteira**

Run: `docker compose run --rm web python manage.py test`
Expected: PASS — projeto todo verde.

---

# Deploy no PythonAnywhere

- [ ] **1. Confirmar o Python da web app** — precisa ser 3.10+. Se for menor, trocar **antes** de qualquer coisa.
- [ ] **2. `pip install -r requirements.txt`** no virtualenv da web app.
- [ ] **3. Gerar as chaves VAPID DE PRODUÇÃO no servidor** (`python manage.py gerar_chaves_vapid`) e colar no `.env` do PythonAnywhere. **Não reaproveitar as chaves de desenvolvimento** — a chave privada de dev apareceu em transcrição de sessão e não vale mais como segredo. Depois de gerar, guardar num cofre: gerar de novo invalida todas as inscrições.
- [ ] **4. `python manage.py migrate`**
- [ ] **5. `python manage.py collectstatic`** — o manifest e os ícones precisam ir para o `STATIC_ROOT`.
- [ ] **6. Reload da web app.**
- [ ] **7. Conferir `https://pcf.pythonanywhere.com/sw.js`** — tem que responder JavaScript, não 404.
- [ ] **8. Instalar num celular real e usar o botão "Enviar notificação de teste".**

---

# Status de execução (2026-08-16)

**Tasks 1 a 15 implementadas e verdes.** 41 testes novos em `notificacoes/`,
suíte completa em 345 testes. As 9 falhas restantes (4 failures + 5 errors em
`adm`, `ronda`, `supply`) são **pré-existentes** — confirmado rodando a mesma
suíte com as alterações em stash, antes de qualquer código deste plano.

**Falta apenas o aceite manual**, que exige aparelho físico: instalar num Android
e num iPhone reais e confirmar o botão "Enviar notificação de teste" com o app
fechado.

## Descobertas da execução

1. **`supply/models.py:4` faz `from voluntario.views import LISTA_AREAS`** — um
   módulo de models importando views. Isso coloca `voluntario/views.py` na cadeia
   de carregamento dos apps. Um import de `notificacoes.services` no topo desse
   arquivo faria **o site inteiro** deixar de subir se o `pywebpush` não estivesse
   instalado. Por isso os três gatilhos de request (`voluntario`, `supply`,
   `forms_pcf`) importam `enviar_push_async` **dentro da função**, não no topo.
   Com isso, dependência faltando quebra só o push. Os testes correspondentes
   fazem patch em `notificacoes.services.enviar_push_async`, não no módulo da view.
2. **`docker compose restart` não recria o container a partir da imagem nova.**
   Depois de mexer no `requirements.txt`, use
   `docker compose up -d --force-recreate web`. O `restart` sobe a imagem velha e
   dá `ModuleNotFoundError` enganoso.
3. **A rota da enquete é `/sabado/responder/<id>/`**, não `/sabado/`. O push do
   gatilho 1 leva direto para o formulário da pessoa.
4. **O WhiteNoise já serve `.webmanifest`** com `Content-Type:
   application/manifest+json`, sem configuração extra. Era dúvida em aberto no spec.
5. **O logo tem 246×248px.** Os ícones de 512 são upscale com LANCZOS. Funciona,
   mas trocar por um logo maior melhora o resultado.

# Notas de auto-revisão

**Correções feitas no spec durante a escrita deste plano:**

1. **`Voluntario.objects.ativos()` já existe** e filtra `data_saida__isnull=True` **e** `is_active=True`. O spec mandava filtrar `data_saida` na mão, o que incluiria login desativado. O plano usa o manager em todo lugar.
2. **Logo de origem tem 246×248px.** O spec assumia que gerar 512×512 era trivial; na prática é upscale. A Task 1 avisa no output do comando.
3. **`Dockerfile` usa `python:3.11-slim`** — atende o piso de 3.10 do `pywebpush` no ambiente local. Só o PythonAnywhere fica por verificar.
4. **A view de avisos usa `enviar_push` síncrono**, não a versão async, porque thread daemon deixa o teste instável e o gestor espera a contagem na tela. Registrado na Task 12 para não ser "corrigido" depois.

**Requisito do spec não coberto por task, de propósito:** a auditoria de responsividade tela a tela. O spec já a declara fora de escopo, como trabalho separado.

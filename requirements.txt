asgiref==3.11.0
diff-match-patch==20241021
Django==4.2.27
django-import-export==4.4.0
et_xmlfile==2.0.0
openpyxl==3.1.5
pillow==11.3.0
python-decouple==3.8
sqlparse==0.5.5
tablib==3.9.0
typing_extensions==4.15.0
whitenoise==6.11.0
tzdata==2025.3

# Revistinha do CR/RE — PDF gerado no servidor.
# xhtml2pdf é Python puro (não precisa de cairo/pango como o WeasyPrint),
# por isso roda igual no Windows e no PythonAnywhere.
xhtml2pdf==0.2.17
reportlab==4.5.1

# Robô de editais: lê RSS e páginas HTML das fontes cadastradas.
requests==2.34.2
feedparser==6.0.14
beautifulsoup4==4.15.0

# Varredura na web (comando varrer_editais): é a perna do robô que descobre
# edital em site que ninguém mapeou. Usa DuckDuckGo porque não pede chave nem
# cartão — Google e Bing cobram, e o projeto não tem orçamento para isso.
# Se um dia parar de funcionar (bloqueio por IP de datacenter, por exemplo),
# o robô continua rodando com as fontes fixas: cada consulta que falha grava o
# motivo e segue, sem derrubar a coleta.
ddgs==9.14.4

# Notificações push da PWA (Web Push / VAPID). Traz junto py-vapid, http-ece e
# cryptography. Exige Python 3.10+ — o Dockerfile usa 3.11, mas CONFIRA a versão
# da web app no PythonAnywhere antes de subir.
pywebpush==2.4.0
